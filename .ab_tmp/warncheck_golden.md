# 测试规程

### PROC-001：初始状态下，普通用户新建导出申请，验证任务状态变更为草稿
**业务定位**：正向流程 ｜ 文件导出任务（草稿）
**覆盖需求**：T-097（4.12.1）

**Given**
1. 文件导出任务.任务状态 = (初始)

**When**
1. 新建导出申请 by 普通用户

**Then**
1. 导出任务创建，状态初始化为草稿 (behavior)

### PROC-002：当前导出方式配置值变更时，验证导出方式显示为修改后的值
**业务定位**：正向流程 ｜ 基础数据-文件导出任务
**覆盖需求**：EO-ATC-004（4.12.1）

**Given**
1. 文件导出任务 状态 = 导出方式为当前值 (当前导出方式配置值)

**When**
1. 导航至文件导出任务配置入口
2. 修改导出方式

**Then**
1. 文件导出任务.导出方式 显示为修改后的值 (state)

### PROC-003：初始状态下，普通用户新建导入申请，验证任务状态变更为草稿
**业务定位**：正向流程 ｜ 导入任务（草稿）
**覆盖需求**：T-090（4.5.1（1））

**Given**
1. 导入任务.任务状态 = (初始)

**When**
1. 新建导入申请 by 普通用户

**Then**
1. 导入任务创建，状态初始化为草稿 (behavior)

### PROC-004：当前任务级别配置值变更时，验证任务级别配置为A级的效果已生效
**业务定位**：正向流程 ｜ 基础数据-导入任务
**覆盖需求**：EO-ATC-001（4.5.1（1））

**Given**
1. 导入任务 状态 = 任务级别为当前值 (当前任务级别配置值)

**When**
1. 导航至导入任务配置入口
2. 修改任务级别为A级

**Then**
1. 配置任务级别=A级的效果已生效 (behavior) [BR: BR-011]
2. [BR-011]正面: 任务级别为A级无需审批，提交后直接进入待执行；B级需经过一级审批；C级需经过二级审批 (behavior) [BR: BR-011] [cross: 载体登记任务,载体归档任务,载体移交任务,载体留存任务,载体回收任务,载体外送任务,文件导出任务,文件扫描任务]
3. 导入任务.任务级别 [BR-011]负面: 不满足条件时规则不触发 (behavior) [BR: BR-011] [cross: 载体登记任务,载体归档任务,载体移交任务,载体留存任务,载体回收任务,载体外送任务,文件导出任务,文件扫描任务]

### PROC-005：当前任务级别配置值变更时，验证任务级别配置为B级的效果已生效
**业务定位**：正向流程 ｜ 基础数据-导入任务
**覆盖需求**：EO-ATC-001（4.5.1（1））

**Given**
1. 导入任务 状态 = 任务级别为当前值 (当前任务级别配置值)

**When**
1. 导航至导入任务配置入口
2. 修改任务级别为B级

**Then**
1. 配置任务级别=B级的效果已生效 (behavior) [BR: BR-011]
2. [BR-011]正面: 任务级别为A级无需审批，提交后直接进入待执行；B级需经过一级审批；C级需经过二级审批 (behavior) [BR: BR-011] [cross: 载体登记任务,载体归档任务,载体移交任务,载体留存任务,载体回收任务,载体外送任务,文件导出任务,文件扫描任务]
3. 导入任务.任务级别 [BR-011]负面: 不满足条件时规则不触发 (behavior) [BR: BR-011] [cross: 载体登记任务,载体归档任务,载体移交任务,载体留存任务,载体回收任务,载体外送任务,文件导出任务,文件扫描任务]

### PROC-006：当前任务级别配置值变更时，验证任务级别配置为C级的效果已生效
**业务定位**：正向流程 ｜ 基础数据-导入任务
**覆盖需求**：EO-ATC-001（4.5.1（1））

**Given**
1. 导入任务 状态 = 任务级别为当前值 (当前任务级别配置值)

**When**
1. 导航至导入任务配置入口
2. 修改任务级别为C级

**Then**
1. 配置任务级别=C级的效果已生效 (behavior) [BR: BR-011]
2. [BR-011]正面: 任务级别为A级无需审批，提交后直接进入待执行；B级需经过一级审批；C级需经过二级审批 (behavior) [BR: BR-011] [cross: 载体登记任务,载体归档任务,载体移交任务,载体留存任务,载体回收任务,载体外送任务,文件导出任务,文件扫描任务]
3. 导入任务.任务级别 [BR-011]负面: 不满足条件时规则不触发 (behavior) [BR: BR-011] [cross: 载体登记任务,载体归档任务,载体移交任务,载体留存任务,载体回收任务,载体外送任务,文件导出任务,文件扫描任务]

### PROC-007：初始状态下，普通用户新建扫描申请，验证任务状态变更为草稿
**业务定位**：正向流程 ｜ 文件扫描任务（草稿）
**覆盖需求**：T-098（4.13.1）

**Given**
1. 文件扫描任务.任务状态 = (初始)

**When**
1. 新建扫描申请 by 普通用户

**Then**
1. 扫描任务创建，状态初始化为草稿 (behavior)

### PROC-008：当前文件级别配置值变更时，验证文件级别显示为修改后的值
**业务定位**：正向流程 ｜ 基础数据-文件扫描任务
**覆盖需求**：EO-ATC-005（4.13.1）

**Given**
1. 文件扫描任务 状态 = 文件级别为当前值 (当前文件级别配置值)

**When**
1. 导航至文件扫描任务配置入口
2. 修改文件级别

**Then**
1. 文件扫描任务.文件级别 显示为修改后的值 (state)

### PROC-009：初始状态下，系统管理员新建用户，验证用户状态变更为正常
**业务定位**：正向流程 ｜ 用户（正常）
**覆盖需求**：T-100（4.14.1（1））

**Given**
1. 用户.用户状态 = (初始)

**When**
1. 新建用户 by 系统管理员

**Then**
1. 用户创建，状态初始化为正常 (behavior)

### PROC-010：用户处于正常状态时，系统管理员锁定用户，验证状态变更为锁定
**业务定位**：正向流程 ｜ 用户（锁定）
**覆盖需求**：T-086（4.14.1（4））

**Given**
1. 用户.用户状态 = 正常 (用户处于正常状态)

**When**
1. 锁定用户 by 系统管理员

**Then**
1. 状态流转：正常→锁定 (state)
2. 锁定后不得登录系统，状态变更为锁定 (behavior)
**依赖**：PROC-009

### PROC-011：用户处于锁定状态时，系统管理员解锁用户，验证用户状态变更为正常
**业务定位**：正向流程 ｜ 用户（正常）
**覆盖需求**：T-087（4.14.1（5））

**Given**
1. 用户.用户状态 = 锁定 (用户处于锁定状态)

**When**
1. 解锁用户 by 系统管理员

**Then**
1. 状态流转：锁定→正常 (state)
2. 用户.用户状态 解锁后可以登录系统 (behavior)
**依赖**：PROC-009

### PROC-012：用户连续输错3次密码时，系统自动锁定用户，验证用户状态变更为锁定
**业务定位**：正向流程 ｜ 用户（锁定）
**覆盖需求**：T-088（4.3.1（2））；CO-005（4.3.1（2））

**Given**
1. 用户.用户状态 = 正常

**When**
1. 连续输错密码锁定 by 系统

**Then**
1. 状态流转：正常→锁定 (state)
2. 用户.用户状态 系统自动将此用户锁定 (behavior)
3. 用户.用户状态 用户连续输错3次密码，系统自动将此用户锁定 (behavior)
**依赖**：PROC-009

### PROC-013：用户接近截止时间连续输错密码时，系统自动锁定用户，验证状态转换为锁定
**业务定位**：时效约束 ｜ 用户（锁定）
**覆盖需求**：T-088（4.3.1（2））；CO-005（4.3.1（2））

**Given**
1. 用户.用户状态 = 正常 (时间边界条件: 接近截止时间)

**When**
1. 连续输错密码锁定(时间边界) by 系统

**Then**
1. 状态转换为锁定(时间边界生效) (state)
**依赖**：PROC-009, PROC-012

### PROC-014：用户超过截止时间尝试连续输错密码锁定时，验证操作不可执行或状态不变
**业务定位**：时效约束 ｜ 用户
**覆盖需求**：T-088（4.3.1（2））；CO-005（4.3.1（2））

**Given**
1. 用户.用户状态 = 正常 (时间过期条件: 超过截止时间)

**When**
1. 尝试连续输错密码锁定(已过期) by 系统

**Then**
1. 操作不可执行或状态不变 (behavior)
**依赖**：PROC-009, PROC-012

### PROC-015：持有人载体到期不进行处理时，系统自动限制用户功能
**业务定位**：正向流程 ｜ 用户（正常）
**覆盖需求**：T-089（4.6.5；4.9.3）；CO-004（4.6.5；4.9.3）

**Given**
1. 用户.用户状态 = 正常
2. 约束：持有人载体到期不进行处理

**When**
1. 功能限制 by 系统

**Then**
1. 用户.用户状态 系统自动将此载体持有人用户进行功能限制，不能发起登记、导入、导出、扫描流程 (behavior)
**依赖**：PROC-009
**弱依赖**：PROC-016, PROC-017

### PROC-016：系统创建载体时，验证载体状态初始化为已登记
**业务定位**：正向流程 ｜ 载体（已登记）
**覆盖需求**：T-099（4.6.3）

**Given**
1. 载体.载体状态 = (初始)

**When**
1. 载体创建 by 系统

**Then**
1. 载体由登记执行完成产生，状态初始化为已登记 (behavior)

### PROC-017：载体持有时间到期前12小时，系统自动发送到期提醒，持有人可选择处理
**业务定位**：正向流程 ｜ 载体（已登记）
**覆盖需求**：T-085（4.6.5；4.9.3）

**Given**
1. 载体.载体状态 = 已登记
2. 约束：载体持有时间到期前12小时

**When**
1. 载体到期提醒 by 系统

**Then**
1. 载体.载体状态 系统自动向持有人发送载体到期提醒；持有人可选择将载体进行归档、移交、回收、留存或者外送；到期不处理系统自动将此载体持有人用户进行功能限制 (behavior)
**依赖**：PROC-016

### PROC-018：载体接近截止时间时，系统自动发送到期提醒，验证状态转换为已登记
**业务定位**：时效约束 ｜ 载体（已登记）
**覆盖需求**：T-085（4.6.5；4.9.3）

**Given**
1. 载体.载体状态 = 已登记 (时间边界条件: 接近截止时间)

**When**
1. 载体到期提醒(时间边界) by 系统

**Then**
1. 状态转换为已登记(时间边界生效) (state)
**依赖**：PROC-016, PROC-017

### PROC-019：载体超过截止时间尝试发送到期提醒时，验证操作不可执行或状态不变
**业务定位**：时效约束 ｜ 载体
**覆盖需求**：T-085（4.6.5；4.9.3）

**Given**
1. 载体.载体状态 = 已登记 (时间过期条件: 超过截止时间)

**When**
1. 尝试载体到期提醒(已过期) by 系统

**Then**
1. 操作不可执行或状态不变 (behavior)
**依赖**：PROC-016, PROC-017

### PROC-020：载体已存在且处于已登记状态时，申请人查询载体台账，验证能够看到本人名下的载体列表
**业务定位**：数据操作 ｜ 载体
**覆盖需求**：EO-CRU-067（4.6.3；4.7.3）

**Given**
1. 载体已存在，处于已登记状态

**When**
1. 导航至载体页面
2. 查询载体台账

**Then**
1. 申请人能够看到本人名下的载体列表 (behavior)
**依赖**：PROC-016

### PROC-021：载体管理员提交含违规值的载体表单，验证载体信息录入页面校验失败并提示错误
**业务定位**：字段校验 ｜ 载体
**覆盖需求**：FIELD-VAL-载体

**Given**
1. 载体 状态 = 可编辑 (载体信息录入页面已打开)

**When**
1. 导航至载体新增/编辑页面
2. 提交含违规值的载体表单 by 载体管理员

**Then**
1. 校验失败，提示'载体级别必填，不能为空' (behavior)
2. 校验失败，提示'载体级别选择错误，请选择A级、B级或C级' (behavior)
3. 校验失败，提示'载体类型必填，不能为空' (behavior)
4. 校验失败，提示'载体类型选择错误，请选择光盘、纸质等' (behavior)
5. 校验失败，提示'人员部门必填，不能为空' (behavior)
6. 校验失败，提示'持有时间格式错误，应为数字' (behavior)
7. 校验失败，提示'持有时间不在1~48小时之间' (behavior)

### PROC-022：普通用户新建归档申请，验证任务状态转换为草稿
**业务定位**：正向流程 ｜ 载体归档任务（草稿）
**覆盖需求**：T-092（4.7.1）

**Given**
1. 载体归档任务.任务状态 = (初始)

**When**
1. 新建归档申请 by 普通用户

**Then**
1. 归档任务创建，状态初始化为草稿 (behavior)

### PROC-023：载体归档任务页面新增归档申请，验证进入归档申请页面并录入申请内容
**业务定位**：数据操作 ｜ 载体归档任务
**覆盖需求**：EO-CRU-016（4.7.1）

**When**
1. 导航至载体归档任务页面
2. 新增归档申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入；包括载体编号、载体名称、文件名、载体级别、载体类型、人员部门等)；备注(字符；可选项)

**Then**
1. 进入归档申请页面，可录入申请内容 (behavior)
**依赖**：PROC-022

### PROC-024：载体归档任务处于草稿状态时编辑申请，验证可修改已存在的归档申请记录
**业务定位**：数据操作 ｜ 载体归档任务
**覆盖需求**：EO-CRU-017（4.4（2））

**Given**
1. 载体归档任务已存在，处于草稿状态

**When**
1. 导航至载体归档任务页面
2. 编辑归档申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入；包括载体编号、载体名称、文件名、载体级别、载体类型、人员部门等)；备注(字符；可选项)

**Then**
1. 可对已存在的归档申请记录进行修改 (behavior)
**依赖**：PROC-022

### PROC-025：载体归档任务处于草稿状态时暂存申请，验证保存为草稿状态
**业务定位**：数据操作 ｜ 载体归档任务
**覆盖需求**：EO-CRU-018（4.4）

**Given**
1. 载体归档任务已存在，处于草稿状态

**When**
1. 导航至载体归档任务页面
2. 暂存归档申请

**Then**
1. 保存为草稿状态 (behavior)
**依赖**：PROC-022

### PROC-026：载体归档任务处于草稿状态时查询，验证申请监控页面显示本人提交的任务列表及状态
**业务定位**：数据操作 ｜ 载体归档任务
**覆盖需求**：EO-CRU-019（4.7.4）

**Given**
1. 载体归档任务已存在，处于草稿状态

**When**
1. 导航至载体归档任务页面
2. 查询归档任务

**Then**
1. 申请监控页面看到本人提交的载体归档任务列表及当前状态 (behavior)
**依赖**：PROC-022

### PROC-027：草稿状态下下载归档任务，验证批量下载保存功能
**业务定位**：数据操作 ｜ 载体归档任务
**覆盖需求**：EO-CRU-020（4.7.4）

**Given**
1. 载体归档任务已存在，处于草稿状态

**When**
1. 导航至载体归档任务页面
2. 下载归档任务

**Then**
1. 能够将载体归档任务批量下载保存 (behavior)
**依赖**：PROC-022

### PROC-028：草稿状态下查看归档任务详情，验证提供记录的详细信息
**业务定位**：数据操作 ｜ 载体归档任务
**覆盖需求**：EO-CRU-021（4.4（4））

**Given**
1. 载体归档任务已存在，处于草稿状态

**When**
1. 导航至载体归档任务页面
2. 查看归档任务详情

**Then**
1. 提供记录的详细信息 (behavior)
**依赖**：PROC-022

### PROC-029：草稿状态下删除归档申请，验证业务允许情况下删除记录并实时刷新列表
**业务定位**：数据操作 ｜ 载体归档任务
**覆盖需求**：EO-CRU-022（4.4（5））

**Given**
1. 载体归档任务已存在，处于草稿状态

**When**
1. 导航至载体归档任务页面
2. 删除归档申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)
**依赖**：PROC-022

### PROC-030：提交含违规值的载体归档任务表单，验证申请事由、载体信息和备注字段的校验失败提示
**业务定位**：字段校验 ｜ 载体归档任务
**覆盖需求**：FIELD-VAL-载体归档任务

**Given**
1. 载体归档任务 状态 = 可编辑 (载体归档任务信息录入页面已打开)

**When**
1. 导航至载体归档任务新增/编辑页面
2. 提交含违规值的载体归档任务表单 by 普通用户

**Then**
1. 载体归档任务.申请事由 校验失败，提示'最长30字符' (behavior)
2. 载体归档任务.载体信息 校验失败，提示'必填项不能为空，从申请人名下载体中选择' (behavior)
3. 载体归档任务.备注 校验失败，提示'可选项，无需输入' (behavior)

### PROC-031：新建外送申请，验证任务状态转换为草稿并初始化
**业务定位**：正向流程 ｜ 载体外送任务（草稿）
**覆盖需求**：T-096（4.11.1）

**Given**
1. 载体外送任务.任务状态 = (初始)

**When**
1. 新建外送申请 by 普通用户

**Then**
1. 外送任务创建，状态初始化为草稿 (behavior)

### PROC-032：外送申请页面，录入申请内容，验证页面功能正常
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-044（4.11.1）

**When**
1. 导航至载体外送任务页面
2. 新增外送申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；外送单位(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入)；文件内容(字符；必填项)；备注(字符；可选项)

**Then**
1. 进入外送申请页面，可录入申请内容 (behavior)
**依赖**：PROC-031

### PROC-033：草稿状态下，编辑外送申请，验证可修改记录
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-045（4.4（2））

**Given**
1. 载体外送任务已存在，处于草稿状态

**When**
1. 导航至载体外送任务页面
2. 编辑外送申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；外送单位(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入)；文件内容(字符；必填项)；备注(字符；可选项)

**Then**
1. 可对已存在的外送申请记录进行修改 (behavior)
**依赖**：PROC-031

### PROC-034：草稿状态下，暂存外送申请，验证保存为草稿状态
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-046（4.4）

**Given**
1. 载体外送任务已存在，处于草稿状态

**When**
1. 导航至载体外送任务页面
2. 暂存外送申请

**Then**
1. 保存为草稿状态 (behavior)
**依赖**：PROC-031

### PROC-035：草稿状态下，查询外送任务，验证申请监控页面显示任务列表及状态
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-047（4.11.4）

**Given**
1. 载体外送任务已存在，处于草稿状态

**When**
1. 导航至载体外送任务页面
2. 查询外送任务

**Then**
1. 申请监控页面看到本人提交的载体外送任务列表及当前状态 (behavior)
**依赖**：PROC-031

### PROC-036：草稿状态下，下载外送任务，验证批量下载保存功能
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-048（4.11.4）

**Given**
1. 载体外送任务已存在，处于草稿状态

**When**
1. 导航至载体外送任务页面
2. 下载外送任务

**Then**
1. 能够将载体外送任务批量下载保存 (behavior)
**依赖**：PROC-031

### PROC-037：草稿状态下，查看载体外送任务详情，验证详细信息提供
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-049（4.4（4））

**Given**
1. 载体外送任务已存在，处于草稿状态

**When**
1. 导航至载体外送任务页面
2. 查看外送任务详情

**Then**
1. 提供记录的详细信息 (behavior)
**依赖**：PROC-031

### PROC-038：草稿状态下，下载载体外送回执单，验证审批通过后可查看和下载
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-050（4.11.2）

**Given**
1. 载体外送任务已存在，处于草稿状态

**When**
1. 导航至载体外送任务页面
2. 下载外送回执单

**Then**
1. 审批通过后回执单可以在回执单菜单进行查看和下载 (behavior)
**依赖**：PROC-031

### PROC-039：草稿状态下，删除载体外送申请，验证业务允许情况下删除并刷新列表
**业务定位**：数据操作 ｜ 载体外送任务
**覆盖需求**：EO-CRU-051（4.4（5））

**Given**
1. 载体外送任务已存在，处于草稿状态

**When**
1. 导航至载体外送任务页面
2. 删除外送申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)
**依赖**：PROC-031

### PROC-040：载体外送任务信息录入页面打开时，提交含违规值的载体外送任务表单，验证校验失败提示
**业务定位**：字段校验 ｜ 载体外送任务
**覆盖需求**：FIELD-VAL-载体外送任务

**Given**
1. 载体外送任务 状态 = 可编辑 (载体外送任务信息录入页面已打开)

**When**
1. 导航至载体外送任务新增/编辑页面
2. 提交含违规值的载体外送任务表单 by 普通用户

**Then**
1. 载体外送任务.申请事由 校验失败，提示'必填项不能为空' (behavior)
2. 载体外送任务.申请事由 校验失败，提示'最长30字符' (behavior)
3. 校验失败，提示'备注为可选项，无需填写' (behavior)
4. 校验失败，提示'备注长度超过最大限制' (behavior)

### PROC-041：初始状态下，新建回收申请，验证状态转换为草稿
**业务定位**：正向流程 ｜ 载体回收任务（草稿）
**覆盖需求**：T-095（4.10.1）

**Given**
1. 载体回收任务.任务状态 = (初始)

**When**
1. 新建回收申请 by 普通用户

**Then**
1. 回收任务创建，状态初始化为草稿 (behavior)

### PROC-042：载体回收任务操作入口可用时，验证进入回收申请页面可录入申请内容
**业务定位**：数据操作 ｜ 载体回收任务
**覆盖需求**：EO-CRU-037（4.10.1）

**When**
1. 导航至载体回收任务页面
2. 新增回收申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入)；备注(字符；可选项)

**Then**
1. 进入回收申请页面，可录入申请内容 (behavior)
**依赖**：PROC-041

### PROC-043：载体回收任务处于草稿状态时，验证可对已存在的回收申请记录进行修改
**业务定位**：数据操作 ｜ 载体回收任务
**覆盖需求**：EO-CRU-038（4.4（2））

**Given**
1. 载体回收任务已存在，处于草稿状态

**When**
1. 导航至载体回收任务页面
2. 编辑回收申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入)；备注(字符；可选项)

**Then**
1. 可对已存在的回收申请记录进行修改 (behavior)
**依赖**：PROC-041

### PROC-044：载体回收任务处于草稿状态时，验证暂存回收申请后保存为草稿状态
**业务定位**：数据操作 ｜ 载体回收任务
**覆盖需求**：EO-CRU-039（4.4）

**Given**
1. 载体回收任务已存在，处于草稿状态

**When**
1. 导航至载体回收任务页面
2. 暂存回收申请

**Then**
1. 保存为草稿状态 (behavior)
**依赖**：PROC-041

### PROC-045：载体回收任务处于草稿状态时，验证查询回收任务看到本人提交的任务列表及状态
**业务定位**：数据操作 ｜ 载体回收任务
**覆盖需求**：EO-CRU-040（4.10.4）

**Given**
1. 载体回收任务已存在，处于草稿状态

**When**
1. 导航至载体回收任务页面
2. 查询回收任务

**Then**
1. 申请监控页面看到本人提交的载体回收任务列表及当前状态 (behavior)
**依赖**：PROC-041

### PROC-046：载体回收任务处于草稿状态时，验证能够将载体回收任务批量下载保存
**业务定位**：数据操作 ｜ 载体回收任务
**覆盖需求**：EO-CRU-041（4.10.4）

**Given**
1. 载体回收任务已存在，处于草稿状态

**When**
1. 导航至载体回收任务页面
2. 下载回收任务

**Then**
1. 能够将载体回收任务批量下载保存 (behavior)
**依赖**：PROC-041

### PROC-047：草稿状态下，查看载体回收任务详情，验证详细信息提供
**业务定位**：数据操作 ｜ 载体回收任务
**覆盖需求**：EO-CRU-042（4.4（4））

**Given**
1. 载体回收任务已存在，处于草稿状态

**When**
1. 导航至载体回收任务页面
2. 查看回收任务详情

**Then**
1. 提供记录的详细信息 (behavior)
**依赖**：PROC-041

### PROC-048：草稿状态下，删除载体回收申请，验证删除记录及数据刷新
**业务定位**：数据操作 ｜ 载体回收任务
**覆盖需求**：EO-CRU-043（4.4（5））

**Given**
1. 载体回收任务已存在，处于草稿状态

**When**
1. 导航至载体回收任务页面
2. 删除回收申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)
**依赖**：PROC-041

### PROC-049：可编辑状态下，提交含违规值的载体回收任务表单，验证校验失败提示
**业务定位**：字段校验 ｜ 载体回收任务
**覆盖需求**：FIELD-VAL-载体回收任务

**Given**
1. 载体回收任务 状态 = 可编辑 (载体回收任务信息录入页面已打开)

**When**
1. 导航至载体回收任务新增/编辑页面
2. 提交含违规值的载体回收任务表单 by 普通用户

**Then**
1. 载体回收任务.申请事由 校验失败，提示'必填项不能为空' (behavior)
2. 载体回收任务.申请事由 校验失败，提示'最长30字符' (behavior)

### PROC-050：初始状态下，新建登记申请，验证状态转换为草稿
**业务定位**：正向流程 ｜ 载体登记任务（草稿）
**覆盖需求**：T-091（4.6.1）

**Given**
1. 载体登记任务.任务状态 = (初始)

**When**
1. 新建登记申请 by 普通用户

**Then**
1. 登记任务创建，状态初始化为草稿 (behavior)

### PROC-051：级别配置变更事件触发，验证级别显示为修改后的值
**业务定位**：正向流程 ｜ 载体登记任务
**覆盖需求**：EO-ATC-003（4.6.1）

**Given**
1. 载体登记任务 状态 = 级别为当前值 (当前级别配置值)

**When**
1. 导航至载体登记任务配置入口
2. 修改级别

**Then**
1. 载体登记任务.级别 显示为修改后的值 (state)

### PROC-052：载体类别配置变更后，验证载体类别显示为修改后的值
**业务定位**：正向流程 ｜ 载体登记任务
**覆盖需求**：EO-ATC-002（4.6.1）

**Given**
1. 载体登记任务 状态 = 载体类别为当前值 (当前载体类别配置值)

**When**
1. 导航至载体登记任务配置入口
2. 修改载体类别

**Then**
1. 载体登记任务.载体类别 显示为修改后的值 (state)

### PROC-053：进入载体登记申请页面，验证可录入申请内容
**业务定位**：数据操作 ｜ 载体登记任务
**覆盖需求**：EO-CRU-009（4.6.1）

**When**
1. 导航至载体登记任务页面
2. 新增登记申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成，规则为:任务类型+申请时间+流水序号)；申请时间(字符；必填项；系统自动生成)；载体信息(必填项；添加载体后可看到载体快照)；文件或资料名称(字符；必填项；30个字符)；载体类别(必填项；下拉选项:光盘、纸质)；载体来源(字符；必填项)；原载体编号(字符；必填项；20个字符)；级别(字符；必填项；只可选:A级、B级、C级)；纸张页数(字符；载体类别为纸质时必填项；数值范围1-9999)；备注(字符；可选项)

**Then**
1. 进入载体登记申请页面，可录入申请内容 (behavior)
**依赖**：PROC-050

### PROC-054：编辑登记申请，验证可对草稿状态记录进行修改
**业务定位**：数据操作 ｜ 载体登记任务
**覆盖需求**：EO-CRU-010（4.4（2））

**Given**
1. 载体登记任务已存在，处于草稿状态

**When**
1. 导航至载体登记任务页面
2. 编辑登记申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成，规则为:任务类型+申请时间+流水序号)；申请时间(字符；必填项；系统自动生成)；载体信息(必填项；添加载体后可看到载体快照)；文件或资料名称(字符；必填项；30个字符)；载体类别(必填项；下拉选项:光盘、纸质)；载体来源(字符；必填项)；原载体编号(字符；必填项；20个字符)；级别(字符；必填项；只可选:A级、B级、C级)；纸张页数(字符；载体类别为纸质时必填项；数值范围1-9999)；备注(字符；可选项)

**Then**
1. 可对已存在的登记申请记录进行修改 (behavior)
**依赖**：PROC-050

### PROC-055：暂存登记申请，验证保存为草稿状态
**业务定位**：数据操作 ｜ 载体登记任务
**覆盖需求**：EO-CRU-011（4.4）

**Given**
1. 载体登记任务已存在，处于草稿状态

**When**
1. 导航至载体登记任务页面
2. 暂存登记申请

**Then**
1. 保存为草稿状态 (behavior)
**依赖**：PROC-050

### PROC-056：查询登记任务，验证申请监控页面显示本人提交的任务列表及状态
**业务定位**：数据操作 ｜ 载体登记任务
**覆盖需求**：EO-CRU-012（4.6.4）

**Given**
1. 载体登记任务已存在，处于草稿状态

**When**
1. 导航至载体登记任务页面
2. 查询登记任务

**Then**
1. 申请监控页面看到本人提交的载体登记任务列表及当前状态 (behavior)
**依赖**：PROC-050

### PROC-057：草稿状态下下载登记任务，验证批量下载保存功能
**业务定位**：数据操作 ｜ 载体登记任务
**覆盖需求**：EO-CRU-013（4.6.4）

**Given**
1. 载体登记任务已存在，处于草稿状态

**When**
1. 导航至载体登记任务页面
2. 下载登记任务

**Then**
1. 能够将载体登记任务批量下载保存 (behavior)
**依赖**：PROC-050

### PROC-058：草稿状态下查看登记任务详情，验证提供记录的详细信息
**业务定位**：数据操作 ｜ 载体登记任务
**覆盖需求**：EO-CRU-014（4.4（4））

**Given**
1. 载体登记任务已存在，处于草稿状态

**When**
1. 导航至载体登记任务页面
2. 查看登记任务详情

**Then**
1. 提供记录的详细信息 (behavior)
**依赖**：PROC-050

### PROC-059：草稿状态下删除登记申请，验证业务允许情况下删除选中记录并实时刷新列表数据
**业务定位**：数据操作 ｜ 载体登记任务
**覆盖需求**：EO-CRU-015（4.4（5））

**Given**
1. 载体登记任务已存在，处于草稿状态

**When**
1. 导航至载体登记任务页面
2. 删除登记申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)
**依赖**：PROC-050

### PROC-060：提交含违规值的载体登记任务表单，验证各字段校验失败并显示相应提示
**业务定位**：字段校验 ｜ 载体登记任务
**覆盖需求**：FIELD-VAL-载体登记任务

**Given**
1. 载体登记任务 状态 = 可编辑 (载体登记任务信息录入页面已打开)

**When**
1. 导航至载体登记任务新增/编辑页面
2. 提交含违规值的载体登记任务表单 by 普通用户

**Then**
1. 载体登记任务.载体信息 校验失败，提示'必填项不能为空，请添加载体后查看载体快照' (behavior)
2. 载体登记任务.文件或资料名称 校验失败，提示'最长30字符' (behavior)
3. 载体登记任务.载体类别 校验失败，提示'必填项不能为空，请选择光盘或纸质' (behavior)
4. 载体登记任务.载体来源 校验失败，提示'必填项不能为空' (behavior)
5. 载体登记任务.原载体编号 校验失败，提示'最长20字符' (behavior)
6. 载体登记任务.级别 校验失败，提示'必选项不能为空，请选择A级、B级或C级' (behavior)
7. 载体登记任务.纸张页数 校验失败，提示'数值范围1-9999' (behavior)
8. 载体登记任务.备注 校验失败，提示'可选项，无需输入' (behavior)

### PROC-061：新建留存申请，验证任务状态转换为草稿并初始化
**业务定位**：正向流程 ｜ 载体留存任务（草稿）
**覆盖需求**：T-094（4.9.1）

**Given**
1. 载体留存任务.任务状态 = (初始)

**When**
1. 新建留存申请 by 普通用户

**Then**
1. 留存任务创建，状态初始化为草稿 (behavior)

### PROC-062：载体留存任务操作入口可用时，新增留存申请，验证进入申请页面并录入内容
**业务定位**：数据操作 ｜ 载体留存任务
**覆盖需求**：EO-CRU-030（4.9.1）

**When**
1. 导航至载体留存任务页面
2. 新增留存申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入)；留存原因(字符；必填项)；留存时间(字符；必填项；单位为小时；只能填入1~48之间的整数)；备注(字符；可选项)

**Then**
1. 进入载体留存申请页面，可录入申请内容 (behavior)
**依赖**：PROC-061

### PROC-063：载体留存任务处于草稿状态时，编辑留存申请，验证可修改记录
**业务定位**：数据操作 ｜ 载体留存任务
**覆盖需求**：EO-CRU-031（4.4（2））

**Given**
1. 载体留存任务已存在，处于草稿状态

**When**
1. 导航至载体留存任务页面
2. 编辑留存申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入)；留存原因(字符；必填项)；留存时间(字符；必填项；单位为小时；只能填入1~48之间的整数)；备注(字符；可选项)

**Then**
1. 可对已存在的留存申请记录进行修改 (behavior)
**依赖**：PROC-061

### PROC-064：载体留存任务处于草稿状态时，暂存留存申请，验证保存为草稿状态
**业务定位**：数据操作 ｜ 载体留存任务
**覆盖需求**：EO-CRU-032（4.4）

**Given**
1. 载体留存任务已存在，处于草稿状态

**When**
1. 导航至载体留存任务页面
2. 暂存留存申请

**Then**
1. 保存为草稿状态 (behavior)
**依赖**：PROC-061

### PROC-065：载体留存任务处于草稿状态时，查询留存任务，验证申请监控页面显示任务列表及状态
**业务定位**：数据操作 ｜ 载体留存任务
**覆盖需求**：EO-CRU-033（4.9.4）

**Given**
1. 载体留存任务已存在，处于草稿状态

**When**
1. 导航至载体留存任务页面
2. 查询留存任务

**Then**
1. 申请监控页面看到本人提交的载体留存任务列表及当前状态 (behavior)
**依赖**：PROC-061

### PROC-066：载体留存任务处于草稿状态时，下载留存任务，验证批量下载保存功能
**业务定位**：数据操作 ｜ 载体留存任务
**覆盖需求**：EO-CRU-034（4.9.4）

**Given**
1. 载体留存任务已存在，处于草稿状态

**When**
1. 导航至载体留存任务页面
2. 下载留存任务

**Then**
1. 能够将载体留存任务批量下载保存 (behavior)
**依赖**：PROC-061

### PROC-067：草稿状态下，查看载体留存任务详情，验证详细信息提供
**业务定位**：数据操作 ｜ 载体留存任务
**覆盖需求**：EO-CRU-035（4.4（4））

**Given**
1. 载体留存任务已存在，处于草稿状态

**When**
1. 导航至载体留存任务页面
2. 查看留存任务详情

**Then**
1. 提供记录的详细信息 (behavior)
**依赖**：PROC-061

### PROC-068：草稿状态下，删除载体留存申请，验证删除记录及数据刷新
**业务定位**：数据操作 ｜ 载体留存任务
**覆盖需求**：EO-CRU-036（4.4（5））

**Given**
1. 载体留存任务已存在，处于草稿状态

**When**
1. 导航至载体留存任务页面
2. 删除留存申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)
**依赖**：PROC-061

### PROC-069：可编辑状态下，提交含违规值的载体留存任务表单，验证校验失败及提示信息
**业务定位**：字段校验 ｜ 载体留存任务
**覆盖需求**：FIELD-VAL-载体留存任务

**Given**
1. 载体留存任务 状态 = 可编辑 (载体留存任务信息录入页面已打开)

**When**
1. 导航至载体留存任务新增/编辑页面
2. 提交含违规值的载体留存任务表单 by 普通用户

**Then**
1. 载体留存任务.申请事由 校验失败，提示'必填项不能为空' (behavior)
2. 载体留存任务.申请事由 校验失败，提示'最长30字符' (behavior)
3. 载体留存任务.留存时间 校验失败，提示'只能填入1~48之间的整数' (behavior)

### PROC-070：初始状态下，新建移交申请，验证状态转换为草稿
**业务定位**：正向流程 ｜ 载体移交任务（草稿）
**覆盖需求**：T-093（4.8.1）

**Given**
1. 载体移交任务.任务状态 = (初始)

**When**
1. 新建移交申请 by 普通用户

**Then**
1. 移交任务创建，状态初始化为草稿 (behavior)

### PROC-071：载体移交任务页面存在，新增移交申请，验证进入申请页面并录入内容
**业务定位**：数据操作 ｜ 载体移交任务
**覆盖需求**：EO-CRU-023（4.8.1）

**When**
1. 导航至载体移交任务页面
2. 新增移交申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入；包括载体编号、载体名称、文件名、载体级别、载体类型、人员部门等)；接收人(字符；必填项；从系统用户中进行选择)；备注(字符；可选项)

**Then**
1. 进入载体移交申请页面，可录入申请内容 (behavior)
**依赖**：PROC-070

### PROC-072：草稿状态下，编辑移交申请，修改已存在记录
**业务定位**：数据操作 ｜ 载体移交任务
**覆盖需求**：EO-CRU-024（4.4（2））

**Given**
1. 载体移交任务已存在，处于草稿状态

**When**
1. 导航至载体移交任务页面
2. 编辑移交申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；载体信息(字符；必填项；从申请人名下载体中选择，载体信息自动带入；包括载体编号、载体名称、文件名、载体级别、载体类型、人员部门等)；接收人(字符；必填项；从系统用户中进行选择)；备注(字符；可选项)

**Then**
1. 可对已存在的移交申请记录进行修改 (behavior)
**依赖**：PROC-070

### PROC-073：草稿状态下，暂存移交申请，保存为草稿状态
**业务定位**：数据操作 ｜ 载体移交任务
**覆盖需求**：EO-CRU-025（4.4）

**Given**
1. 载体移交任务已存在，处于草稿状态

**When**
1. 导航至载体移交任务页面
2. 暂存移交申请

**Then**
1. 保存为草稿状态 (behavior)
**依赖**：PROC-070

### PROC-074：草稿状态下，查询移交任务，看到本人提交的任务列表及状态
**业务定位**：数据操作 ｜ 载体移交任务
**覆盖需求**：EO-CRU-026（4.8.4）

**Given**
1. 载体移交任务已存在，处于草稿状态

**When**
1. 导航至载体移交任务页面
2. 查询移交任务

**Then**
1. 申请监控页面看到本人提交的载体移交任务列表及当前状态 (behavior)
**依赖**：PROC-070

### PROC-075：草稿状态下，下载移交任务，批量下载保存任务
**业务定位**：数据操作 ｜ 载体移交任务
**覆盖需求**：EO-CRU-027（4.8.4）

**Given**
1. 载体移交任务已存在，处于草稿状态

**When**
1. 导航至载体移交任务页面
2. 下载移交任务

**Then**
1. 能够将载体移交任务批量下载保存 (behavior)
**依赖**：PROC-070

### PROC-076：草稿状态下，查看移交任务详情，提供记录详细信息
**业务定位**：数据操作 ｜ 载体移交任务
**覆盖需求**：EO-CRU-028（4.4（4））

**Given**
1. 载体移交任务已存在，处于草稿状态

**When**
1. 导航至载体移交任务页面
2. 查看移交任务详情

**Then**
1. 提供记录的详细信息 (behavior)
**依赖**：PROC-070

### PROC-077：载体移交任务处于草稿状态时，删除申请，验证业务允许情况下删除选中记录且列表数据实时刷新
**业务定位**：数据操作 ｜ 载体移交任务
**覆盖需求**：EO-CRU-029（4.4（5））

**Given**
1. 载体移交任务已存在，处于草稿状态

**When**
1. 导航至载体移交任务页面
2. 删除移交申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)
**依赖**：PROC-070

### PROC-078：载体移交任务信息录入页面打开时，提交含违规值的载体移交任务表单，验证校验失败并提示错误信息
**业务定位**：字段校验 ｜ 载体移交任务
**覆盖需求**：FIELD-VAL-载体移交任务

**Given**
1. 载体移交任务 状态 = 可编辑 (载体移交任务信息录入页面已打开)

**When**
1. 导航至载体移交任务新增/编辑页面
2. 提交含违规值的载体移交任务表单 by 普通用户

**Then**
1. 载体移交任务.申请事由 校验失败，提示'必填项不能为空' (behavior)
2. 载体移交任务.申请事由 校验失败，提示'最长30字符' (behavior)

### PROC-079：导出申请草稿状态下，提交导出申请，验证状态流转为待审批
**业务定位**：正向流程 ｜ 文件导出任务（待审批）
**覆盖需求**：T-064（4.12.1）

**Given**
1. 文件导出任务.任务状态 = 草稿
2. 约束：申请人已填写必填项
3. 约束：导出文件不大于2GB
4. 约束：份数在1~99之间

**When**
1. 提交导出申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 导出申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-001

### PROC-080：导入申请草稿状态下，提交导入申请，验证状态流转为待审批
**业务定位**：正向流程 ｜ 导入任务（待审批）
**覆盖需求**：T-001（4.5.1；4.4（3））

**Given**
1. 导入任务.任务状态 = 草稿
2. 约束：申请人已填写必填项

**When**
1. 提交导入申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 导入申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-003

### PROC-081：扫描申请草稿状态下，提交扫描申请，验证状态流转为待审批
**业务定位**：正向流程 ｜ 文件扫描任务（待审批）
**覆盖需求**：T-073（4.13.1）

**Given**
1. 文件扫描任务.任务状态 = 草稿
2. 约束：申请人已填写必填项
3. 约束：文件页数在1~100之间

**When**
1. 提交扫描申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 扫描申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-007

### PROC-082：载体已登记且归档任务已完成时，进行载体归档，验证状态流转为已归档
**业务定位**：正向流程 ｜ 载体（已归档）
**覆盖需求**：T-082（4.7.3）；CO-001（4.7.3）

**Given**
1. 载体.载体状态 = 已登记 (载体处于已登记状态)
2. 载体.载体状态 = 已登记 (载体归档任务已完成)

**When**
1. 载体归档 by 载体管理员

**Then**
1. 状态流转：已登记→已归档 (state)
2. 载体.载体状态 变为已归档；对已归档载体，无法进行移交、留存、回收、外送操作 (behavior)
**依赖**：PROC-016
**弱依赖**：PROC-159

### PROC-083：载体已登记且回收任务已完成时，进行载体回收，验证状态流转为已回收
**业务定位**：正向流程 ｜ 载体（已回收）
**覆盖需求**：T-083（4.10.3）；CO-002（4.10；4.10.3）

**Given**
1. 载体.载体状态 = 已登记 (载体处于可回收状态)
2. 载体.载体状态 = 已登记 (载体回收任务已完成)

**When**
1. 载体回收 by 载体管理员

**Then**
1. 状态流转：已登记→已回收 (state)
2. 载体.载体状态 变为已回收 (behavior)
**依赖**：PROC-016
**弱依赖**：PROC-167

### PROC-084：载体已登记且外送任务完成后，管理员进行载体外送，验证状态变更为已外送
**业务定位**：正向流程 ｜ 载体（已外送）
**覆盖需求**：T-084（4.11.3）；CO-003（4.11.3）

**Given**
1. 载体.载体状态 = 已登记 (载体处于可外送状态)
2. 载体.载体状态 = 已登记 (载体外送任务已完成)

**When**
1. 载体外送 by 载体管理员

**Then**
1. 状态流转：已登记→已外送 (state)
2. 载体.载体状态 变为已外送 (behavior)
**依赖**：PROC-016
**弱依赖**：PROC-163

### PROC-085：已归档载体尝试转换为已回收，验证操作被拒绝且状态不变
**业务定位**：非法验证 ｜ 载体
**覆盖需求**：RO-IT-001（4.7.3）

**Given**
1. 载体 状态 = 已归档 (载体处于"已归档"状态)

**When**
1. 尝试执行从"已归档"到"已回收"的操作

**Then**
1. 操作被拒绝，对已归档载体，无法进行回收操作 (prompt)
2. 状态仍为已归档 (state)
**依赖**：PROC-082

### PROC-086：已归档载体尝试转换为已外送，验证操作被拒绝且状态不变
**业务定位**：非法验证 ｜ 载体
**覆盖需求**：RO-IT-002（4.7.3）

**Given**
1. 载体 状态 = 已归档 (载体处于"已归档"状态)

**When**
1. 尝试执行从"已归档"到"已外送"的操作

**Then**
1. 操作被拒绝，对已归档载体，无法进行外送操作 (prompt)
2. 状态仍为已归档 (state)
**依赖**：PROC-082

### PROC-087：已回收载体尝试转换为已归档，验证操作被拒绝且状态不变
**业务定位**：非法验证 ｜ 载体
**覆盖需求**：RO-IT-003（4.10.3）

**Given**
1. 载体 状态 = 已回收 (载体处于"已回收"状态)

**When**
1. 尝试执行从"已回收"到"已归档"的操作

**Then**
1. 操作被拒绝，已回收为终态，不可再归档 (prompt)
2. 状态仍为已回收 (state)
**依赖**：PROC-083

### PROC-088：载体登记成功后，验证规则执行使载体记入申请人台账并设置持有时间及到期提醒
**业务定位**：业务规则 ｜ 载体
**覆盖需求**：RO-BR-013（4.6.5）

**Then**
1. [BR-013] 载体登记成功之后，载体记入申请人台账中，持有时间默认为72小时；在到期前12小时，系统会自动向持有人发送载体到期提醒 (behavior) [BR: BR-013]

### PROC-089：持有人到期不处理载体，验证系统自动限制用户功能
**业务定位**：业务规则 ｜ 载体
**覆盖需求**：RO-BR-014（4.6.5）

**Then**
1. [BR-014] 如果持有人到期不进行处理，系统会自动将此载体持有人用户进行功能限制，不能发起登记、导入、导出、扫描流程 (behavior) [BR: BR-014]

### PROC-090：载体留存时间到期前12小时，验证系统自动发送到期提醒并允许操作
**业务定位**：业务规则 ｜ 载体
**覆盖需求**：RO-BR-037（4.9.3）

**Then**
1. [BR-037]正面: 在载体里留存时间到期前12小时，系统会自动向持有人发送载体到期提醒，持有人可选择将载体进行归档、移交、回收、留存或者外送 (behavior) [BR: BR-037]
2. [BR-037]负面: 不满足条件时规则不触发 (behavior) [BR: BR-037]

### PROC-091：草稿状态归档申请提交后，验证状态变更为待审批
**业务定位**：正向流程 ｜ 载体归档任务（待审批）
**覆盖需求**：T-019（4.7.1）

**Given**
1. 载体归档任务.任务状态 = 草稿
2. 载体.载体状态 = 已登记 (载体处于已登记状态)
3. 约束：申请人已填写必填项

**When**
1. 提交归档申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 归档申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-016, PROC-022

### PROC-092：草稿状态外送申请提交后，验证状态变更为待审批
**业务定位**：正向流程 ｜ 载体外送任务（待审批）
**覆盖需求**：T-055（4.11.1）

**Given**
1. 载体外送任务.任务状态 = 草稿
2. 载体.载体状态 = 已登记 (载体处于可外送状态)
3. 约束：申请人已填写必填项

**When**
1. 提交外送申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 外送申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-016, PROC-031

### PROC-093：回收申请草稿状态下，普通用户提交申请，验证任务状态变更为待审批
**业务定位**：正向流程 ｜ 载体回收任务（待审批）
**覆盖需求**：T-046（4.10.1）

**Given**
1. 载体回收任务.任务状态 = 草稿
2. 载体.载体状态 = 已登记 (载体处于可回收状态)
3. 约束：申请人已填写必填项

**When**
1. 提交回收申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 回收申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-016, PROC-041

### PROC-094：登记申请草稿状态下，普通用户提交申请，验证任务状态变更为待审批
**业务定位**：正向流程 ｜ 载体登记任务（待审批）
**覆盖需求**：T-010（4.6.1；4.4（3））

**Given**
1. 载体登记任务.任务状态 = 草稿
2. 约束：申请人已填写必填项

**When**
1. 提交登记申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 登记申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-050

### PROC-095：留存申请草稿状态下，普通用户提交申请，验证任务状态变更为待审批
**业务定位**：正向流程 ｜ 载体留存任务（待审批）
**覆盖需求**：T-037（4.9.1）

**Given**
1. 载体留存任务.任务状态 = 草稿
2. 载体.载体状态 = 已登记 (载体处于可留存状态)
3. 约束：申请人已填写必填项
4. 约束：留存时间在1~48小时之间

**When**
1. 提交留存申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 留存申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-016, PROC-061

### PROC-096：移交申请草稿状态下，普通用户提交申请，验证任务状态变更为待审批
**业务定位**：正向流程 ｜ 载体移交任务（待审批）
**覆盖需求**：T-028（4.8.1）

**Given**
1. 载体移交任务.任务状态 = 草稿
2. 载体.载体状态 = 已登记 (载体处于可移交状态)
3. 约束：申请人已填写必填项

**When**
1. 提交移交申请 by 普通用户

**Then**
1. 状态流转：草稿→待审批 (state)
2. 移交申请提交至审批流程；任务状态变为待审批 (behavior)
**依赖**：PROC-016, PROC-070

### PROC-097：C级导出任务待审批状态下，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 文件导出任务（待二级审批）
**覆盖需求**：T-066（4.12.2）

**Given**
1. 文件导出任务.任务状态 = 待审批 (导出任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-001, PROC-079

### PROC-098：C级导入任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 导入任务（待二级审批）
**覆盖需求**：T-003（4.5.2）

**Given**
1. 导入任务.任务状态 = 待审批 (导入任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-003, PROC-080

### PROC-099：C级扫描任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 文件扫描任务（待二级审批）
**覆盖需求**：T-075（4.13.2）

**Given**
1. 文件扫描任务.任务状态 = 待审批 (扫描任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-007, PROC-081

### PROC-100：C级归档任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 载体归档任务（待二级审批）
**覆盖需求**：T-021（4.7.2）

**Given**
1. 载体归档任务.任务状态 = 待审批 (归档任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-022, PROC-091

### PROC-101：C级外送任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 载体外送任务（待二级审批）
**覆盖需求**：T-057（4.11.2）

**Given**
1. 载体外送任务.任务状态 = 待审批 (外送任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-031, PROC-092

### PROC-102：C级回收任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 载体回收任务（待二级审批）
**覆盖需求**：T-048（4.10.2）

**Given**
1. 载体回收任务.任务状态 = 待审批 (回收任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-041, PROC-093

### PROC-103：C级登记任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 载体登记任务（待二级审批）
**覆盖需求**：T-012（4.6.2）

**Given**
1. 载体登记任务.任务状态 = 待审批 (登记任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-050, PROC-094

### PROC-104：C级留存任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 载体留存任务（待二级审批）
**覆盖需求**：T-039（4.9.2）

**Given**
1. 载体留存任务.任务状态 = 待审批 (留存任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-061, PROC-095

### PROC-105：C级移交任务待审批时，一级审批员审批通过，验证任务状态变更为待二级审批
**业务定位**：分支路径 ｜ 载体移交任务（待二级审批）
**覆盖需求**：T-030（4.8.2）

**Given**
1. 载体移交任务.任务状态 = 待审批 (移交任务处于待审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 一级审批通过（C级送二级） by 一级审批员

**Then**
1. 状态流转：待审批→待二级审批 (state)
2. 若任务级别=C级，则一级审批员通过后任务进入待二级审批状态，待二级审批员审批 (behavior)
**依赖**：PROC-070, PROC-096

### PROC-106：B级导出任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 文件导出任务（审批通过）
**覆盖需求**：T-065（4.12.2）

**Given**
1. 文件导出任务.任务状态 = 待审批 (导出任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后导出申请人可进行载体的导出；任务状态变为审批通过 (behavior)
**依赖**：PROC-001, PROC-079

### PROC-107：C级导出任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 文件导出任务（审批通过）
**覆盖需求**：T-067（4.12.2）

**Given**
1. 文件导出任务.任务状态 = 待二级审批 (导出任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后导出申请人可进行载体的导出；任务状态变为审批通过 (behavior)
**依赖**：PROC-001, PROC-097

### PROC-108：B级导入任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 导入任务（审批通过）
**覆盖需求**：T-002（4.5.2）

**Given**
1. 导入任务.任务状态 = 待审批 (导入任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后导入申请人可进行文件的导入；任务状态变为审批通过 (behavior)
**依赖**：PROC-003, PROC-080

### PROC-109：C级导入任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 导入任务（审批通过）
**覆盖需求**：T-004（4.5.2）

**Given**
1. 导入任务.任务状态 = 待二级审批 (导入任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后导入申请人可进行文件的导入；任务状态变为审批通过 (behavior)
**依赖**：PROC-003, PROC-098

### PROC-110：B级扫描任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 文件扫描任务（审批通过）
**覆盖需求**：T-074（4.13.2）

**Given**
1. 文件扫描任务.任务状态 = 待审批 (扫描任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后扫描申请人可进行文件的扫描；任务状态变为审批通过 (behavior)
**依赖**：PROC-007, PROC-081

### PROC-111：C级扫描任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 文件扫描任务（审批通过）
**覆盖需求**：T-076（4.13.2）

**Given**
1. 文件扫描任务.任务状态 = 待二级审批 (扫描任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后扫描申请人可进行文件的扫描；任务状态变为审批通过 (behavior)
**依赖**：PROC-007, PROC-099

### PROC-112：B级归档任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体归档任务（审批通过）
**覆盖需求**：T-020（4.7.2）

**Given**
1. 载体归档任务.任务状态 = 待审批 (归档任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后归档申请人需将载体交到载体管理员处；任务状态变为审批通过 (behavior)
**依赖**：PROC-022, PROC-091

### PROC-113：C级归档任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体归档任务（审批通过）
**覆盖需求**：T-022（4.7.2）

**Given**
1. 载体归档任务.任务状态 = 待二级审批 (归档任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后归档申请人需将载体交到载体管理员处；任务状态变为审批通过 (behavior)
**依赖**：PROC-022, PROC-100

### PROC-114：B级外送任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体外送任务（审批通过）
**覆盖需求**：T-056（4.11.2）

**Given**
1. 载体外送任务.任务状态 = 待审批 (外送任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后外送申请人需打印载体外送交接单并将回执交给载体管理员；回执单可在回执单菜单查看和下载；任务状态变为审批通过 (behavior)
**依赖**：PROC-031, PROC-092

### PROC-115：C级外送任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体外送任务（审批通过）
**覆盖需求**：T-058（4.11.2）

**Given**
1. 载体外送任务.任务状态 = 待二级审批 (外送任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后外送申请人需打印载体外送交接单并将回执交给载体管理员；任务状态变为审批通过 (behavior)
**依赖**：PROC-031, PROC-101

### PROC-116：B级回收任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体回收任务（审批通过）
**覆盖需求**：T-047（4.10.2）

**Given**
1. 载体回收任务.任务状态 = 待审批 (回收任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后回收申请人将载体移交给载体管理员进行回收；任务状态变为审批通过 (behavior)
**依赖**：PROC-041, PROC-093

### PROC-117：C级回收任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体回收任务（审批通过）
**覆盖需求**：T-049（4.10.2）

**Given**
1. 载体回收任务.任务状态 = 待二级审批 (回收任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后回收申请人将载体移交给载体管理员进行回收；任务状态变为审批通过 (behavior)
**依赖**：PROC-041, PROC-102

### PROC-118：B级登记任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体登记任务（审批通过）
**覆盖需求**：T-011（4.6.2）

**Given**
1. 载体登记任务.任务状态 = 待审批 (登记任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后登记申请人可看到载体已经登记入个人台账；任务状态变为审批通过 (behavior)
**依赖**：PROC-050, PROC-094

### PROC-119：C级登记任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体登记任务（审批通过）
**覆盖需求**：T-013（4.6.2）

**Given**
1. 载体登记任务.任务状态 = 待二级审批 (登记任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后登记申请人可看到载体已经登记入个人台账；任务状态变为审批通过 (behavior)
**依赖**：PROC-050, PROC-103

### PROC-120：B级留存任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体留存任务（审批通过）
**覆盖需求**：T-038（4.9.2）

**Given**
1. 载体留存任务.任务状态 = 待审批 (留存任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后载体持有时间自动增加；任务状态变为审批通过 (behavior)
**依赖**：PROC-061, PROC-095

### PROC-121：C级留存任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体留存任务（审批通过）
**覆盖需求**：T-040（4.9.2）

**Given**
1. 载体留存任务.任务状态 = 待二级审批 (留存任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后载体持有时间自动增加；任务状态变为审批通过 (behavior)
**依赖**：PROC-061, PROC-104

### PROC-122：B级移交任务待审批时，一级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体移交任务（审批通过）
**覆盖需求**：T-029（4.8.2）

**Given**
1. 载体移交任务.任务状态 = 待审批 (移交任务处于待审批状态)
2. 约束：任务级别为B级
3. 分支条件：任务级别=B级

**When**
1. 审批通过 by 一级审批员

**Then**
1. 状态流转：待审批→审批通过 (state)
2. 审批通过后移交申请人需和接收人完成交接；任务状态变为审批通过 (behavior)
**依赖**：PROC-070, PROC-096

### PROC-123：C级移交任务待二级审批时，二级审批员审批通过，验证任务状态变更为审批通过
**业务定位**：分支路径 ｜ 载体移交任务（审批通过）
**覆盖需求**：T-031（4.8.2）

**Given**
1. 载体移交任务.任务状态 = 待二级审批 (移交任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批通过 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批通过 (state)
2. 若任务级别=C级，则二级审批员通过后审批通过后移交申请人需和接收人完成交接；任务状态变为审批通过 (behavior)
**依赖**：PROC-070, PROC-105

### PROC-124：A级导出任务草稿状态下，普通用户提交申请，验证任务状态直接变更为待执行
**业务定位**：分支路径 ｜ 文件导出任务（待执行）
**覆盖需求**：T-070（4.12.2）

**Given**
1. 文件导出任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 约束：导出文件不大于2GB
5. 分支条件：任务级别=A级

**When**
1. 提交导出申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-001, PROC-004

### PROC-125：导出任务审批通过后，载体管理员进入导出执行，验证任务状态变更为待执行
**业务定位**：正向流程 ｜ 文件导出任务（待执行）
**覆盖需求**：T-071（4.12.3）

**Given**
1. 文件导出任务.任务状态 = 审批通过 (导出任务审批通过)

**When**
1. 进入导出执行 by 载体管理员

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 文件导出任务.任务状态 载体管理员在线确认载体已导出；任务进入执行环节 (behavior)
**依赖**：PROC-001, PROC-106, PROC-107

### PROC-126：A级导入任务草稿状态下，普通用户提交申请，验证任务状态直接变更为待执行
**业务定位**：分支路径 ｜ 导入任务（待执行）
**覆盖需求**：T-007（4.5.2）

**Given**
1. 导入任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 分支条件：任务级别=A级

**When**
1. 提交导入申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-003, PROC-004

### PROC-127：导入任务审批通过后，载体管理员进入导入执行，验证任务状态变更为待执行
**业务定位**：正向流程 ｜ 导入任务（待执行）
**覆盖需求**：T-008（4.5.3）

**Given**
1. 导入任务.任务状态 = 审批通过 (导入任务审批通过)

**When**
1. 进入导入执行 by 载体管理员

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 导入任务.任务状态 导入任务进入执行环节；载体管理员实施文件导入 (behavior)
**依赖**：PROC-003, PROC-108, PROC-109

### PROC-128：A级扫描任务草稿状态下，普通用户提交申请，验证任务状态直接变更为待执行
**业务定位**：分支路径 ｜ 文件扫描任务（待执行）
**覆盖需求**：T-079（4.13.2）

**Given**
1. 文件扫描任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 约束：文件页数在1~100之间
5. 分支条件：任务级别=A级

**When**
1. 提交扫描申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-004, PROC-007

### PROC-129：扫描任务审批通过后，载体管理员进入扫描执行，验证任务状态变更为待执行
**业务定位**：正向流程 ｜ 文件扫描任务（待执行）
**覆盖需求**：T-080（4.13.3）

**Given**
1. 文件扫描任务.任务状态 = 审批通过 (扫描任务审批通过)

**When**
1. 进入扫描执行 by 载体管理员

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 文件扫描任务.任务状态 执行人操作扫描设备完成扫描；任务进入执行环节 (behavior)
**依赖**：PROC-007, PROC-110, PROC-111

### PROC-130：A级归档任务草稿状态下，普通用户提交直入申请后，任务状态变更为待执行
**业务定位**：分支路径 ｜ 载体归档任务（待执行）
**覆盖需求**：T-025（4.7.2）

**Given**
1. 载体归档任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 分支条件：任务级别=A级

**When**
1. 提交归档申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-004, PROC-022

### PROC-131：归档任务审批通过后，载体管理员进入执行环节，任务状态变更为待执行
**业务定位**：正向流程 ｜ 载体归档任务（待执行）
**覆盖需求**：T-026（4.7.3）

**Given**
1. 载体归档任务.任务状态 = 审批通过 (归档任务审批通过)

**When**
1. 进入归档执行 by 载体管理员

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 载体归档任务.任务状态 归档任务进入执行环节；载体管理员接收载体并确认归档完成 (behavior)
**依赖**：PROC-022, PROC-112, PROC-113

### PROC-132：A级外送任务草稿状态下，普通用户提交直入申请后，任务状态变更为待执行
**业务定位**：分支路径 ｜ 载体外送任务（待执行）
**覆盖需求**：T-061（4.11.2）

**Given**
1. 载体外送任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 分支条件：任务级别=A级

**When**
1. 提交外送申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-004, PROC-031

### PROC-133：外送任务审批通过且载体管理员接收回执后，进入执行环节，任务状态变更为待执行
**业务定位**：正向流程 ｜ 载体外送任务（待执行）
**覆盖需求**：T-062（4.11.3）

**Given**
1. 载体外送任务.任务状态 = 审批通过 (外送任务审批通过)
2. 载体外送任务.任务状态 = 审批通过 (载体管理员已接收外送交接单回执)

**When**
1. 进入外送执行 by 载体管理员

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 载体外送任务.任务状态 载体管理员在线确认载体已外送；任务进入执行环节 (behavior)
**依赖**：PROC-031, PROC-114, PROC-115

### PROC-134：A级回收任务草稿状态下，普通用户提交直入申请后，任务状态变更为待执行
**业务定位**：分支路径 ｜ 载体回收任务（待执行）
**覆盖需求**：T-052（4.10.2）

**Given**
1. 载体回收任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 分支条件：任务级别=A级

**When**
1. 提交回收申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-004, PROC-041

### PROC-135：回收任务审批通过后，载体管理员进入回收执行，验证任务状态变更为待执行及状态流转
**业务定位**：正向流程 ｜ 载体回收任务（待执行）
**覆盖需求**：T-053（4.10.3）

**Given**
1. 载体回收任务.任务状态 = 审批通过 (回收任务审批通过)

**When**
1. 进入回收执行 by 载体管理员

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 载体回收任务.任务状态 将当前用户所申请回收的载体转交到载体管理员处；载体管理员在线确认载体回收 (behavior)
**依赖**：PROC-041, PROC-116, PROC-117

### PROC-136：A级任务草稿状态下，普通用户提交登记申请，验证任务状态变更为待执行及状态流转
**业务定位**：分支路径 ｜ 载体登记任务（待执行）
**覆盖需求**：T-016（4.6.2）

**Given**
1. 载体登记任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 分支条件：任务级别=A级

**When**
1. 提交登记申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-004, PROC-050

### PROC-137：登记任务审批通过后，载体管理员进入登记执行，验证任务状态变更为待执行及状态流转
**业务定位**：正向流程 ｜ 载体登记任务（待执行）
**覆盖需求**：T-017（4.6.3）

**Given**
1. 载体登记任务.任务状态 = 审批通过 (登记任务审批通过)

**When**
1. 进入登记执行 by 载体管理员

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 载体登记任务.任务状态 登记任务进入执行环节；载体管理员实施载体登记 (behavior)
**依赖**：PROC-050, PROC-118, PROC-119

### PROC-138：A级任务草稿状态下，普通用户提交留存申请，验证任务状态变更为待执行及状态流转
**业务定位**：分支路径 ｜ 载体留存任务（待执行）
**覆盖需求**：T-043（4.9.2）

**Given**
1. 载体留存任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 约束：留存时间在1~48小时之间
5. 分支条件：任务级别=A级

**When**
1. 提交留存申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-004, PROC-061

### PROC-139：留存任务审批通过后，系统进入留存执行，验证任务状态变更为待执行及状态流转
**业务定位**：正向流程 ｜ 载体留存任务（待执行）
**覆盖需求**：T-044（4.9.3）

**Given**
1. 载体留存任务.任务状态 = 审批通过 (留存任务审批通过)

**When**
1. 进入留存执行 by 系统

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 载体留存任务.任务状态 将当前用户所持有的载体持有时间自动增加 (behavior)
**依赖**：PROC-061, PROC-120, PROC-121

### PROC-140：A级任务草稿状态下，普通用户提交移交申请，验证任务状态变更为待执行
**业务定位**：分支路径 ｜ 载体移交任务（待执行）
**覆盖需求**：T-034（4.8.2）

**Given**
1. 载体移交任务.任务状态 = 草稿
2. 约束：任务级别为A级
3. 约束：申请人已填写必填项
4. 分支条件：任务级别=A级

**When**
1. 提交移交申请（A级直入） by 普通用户

**Then**
1. 状态流转：草稿→待执行 (state)
2. A级任务无需审批，提交后直接进入待执行状态 (behavior)
**依赖**：PROC-004, PROC-070

### PROC-141：移交任务审批通过后，普通用户进入移交执行，验证任务状态变更为待执行
**业务定位**：正向流程 ｜ 载体移交任务（待执行）
**覆盖需求**：T-035（4.8.3）

**Given**
1. 载体移交任务.任务状态 = 审批通过 (移交任务审批通过)

**When**
1. 进入移交执行 by 普通用户

**Then**
1. 状态流转：审批通过→待执行 (state)
2. 载体移交任务.任务状态 移交任务进入执行环节；接收人在线确认载体移交 (behavior)
**依赖**：PROC-070, PROC-122, PROC-123

### PROC-142：C级导出任务待二级审批状态下，二级审批员拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 文件导出任务（审批拒绝）
**覆盖需求**：T-068（4.12.2）

**Given**
1. 文件导出任务.任务状态 = 待二级审批 (导出任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 文件导出任务.任务状态 若任务级别=C级且二级审批员拒绝，则导出任务终止；导出申请人需重新提交导出任务 (behavior)
**依赖**：PROC-001, PROC-097

### PROC-143：B级导出任务待审批状态下，一级审批员审批拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 文件导出任务（审批拒绝）
**覆盖需求**：T-069[a]（4.12.2）

**Given**
1. 文件导出任务.任务状态 = 待审批 (导出任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 文件导出任务.任务状态 导出任务终止；导出申请人需重新提交导出任务 (behavior)
**依赖**：PROC-001, PROC-079

### PROC-144：C级导出任务待审批状态下，一级审批员审批拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 文件导出任务（审批拒绝）
**覆盖需求**：T-069[b]（4.12.2）

**Given**
1. 文件导出任务.任务状态 = 待审批 (导出任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 文件导出任务.任务状态 导出任务终止；导出申请人需重新提交导出任务 (behavior)
**依赖**：PROC-001, PROC-079

### PROC-145：导出任务待执行状态下，载体管理员确认导出完成，验证任务状态变更为已完成
**业务定位**：正向流程 ｜ 文件导出任务（已完成）
**覆盖需求**：T-072（4.12；4.12.3）

**Given**
1. 文件导出任务.任务状态 = 待执行
2. 文件导出任务.任务状态 = 待执行 (载体管理员在线确认导出任务完成)

**When**
1. 确认导出完成 by 载体管理员

**Then**
1. 状态流转：待执行→已完成 (state)
2. 文件导出任务.任务状态 任务完成后系统归档任务信息、导出记录及光盘信息；支持光盘编号管理 (behavior)
**依赖**：PROC-001, PROC-124, PROC-125

### PROC-146：C级导入任务待二级审批状态下，二级审批员拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 导入任务（审批拒绝）
**覆盖需求**：T-005（4.5.2）

**Given**
1. 导入任务.任务状态 = 待二级审批 (导入任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 导入任务.任务状态 若任务级别=C级且二级审批员拒绝，则导入任务终止；导入申请人需重新提交导入任务 (behavior)
**依赖**：PROC-003, PROC-098

### PROC-147：B级导入任务待审批状态下，一级审批员审批拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 导入任务（审批拒绝）
**覆盖需求**：T-006[a]（4.5.2）

**Given**
1. 导入任务.任务状态 = 待审批 (导入任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 导入任务.任务状态 导入任务终止；导入申请人需重新提交导入任务 (behavior)
**依赖**：PROC-003, PROC-080

### PROC-148：C级导入任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及终止导入
**业务定位**：分支路径 ｜ 导入任务（审批拒绝）
**覆盖需求**：T-006[b]（4.5.2）

**Given**
1. 导入任务.任务状态 = 待审批 (导入任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 导入任务.任务状态 导入任务终止；导入申请人需重新提交导入任务 (behavior)
**依赖**：PROC-003, PROC-080

### PROC-149：文件导入任务待执行时，普通用户确认导入完成，验证任务状态变更为已完成及任务结束
**业务定位**：正向流程 ｜ 导入任务（已完成）
**覆盖需求**：T-009（4.5.3）

**Given**
1. 导入任务.任务状态 = 待执行
2. 导入任务.任务状态 = 待执行 (载体管理员已完成文件导入)

**When**
1. 确认导入完成 by 普通用户

**Then**
1. 状态流转：待执行→已完成 (state)
2. 导入任务.任务状态 申请人下载导入文件后进行确认，此任务结束；如未确认48小时后系统自动结束并删除导入文件 (behavior)
**依赖**：PROC-003, PROC-126, PROC-127

### PROC-150：文件导入任务接近截止时间时，普通用户确认导入完成，验证任务状态转换为已完成
**业务定位**：时效约束 ｜ 导入任务（已完成）
**覆盖需求**：T-009（4.5.3）

**Given**
1. 导入任务.任务状态 = 待执行 (时间边界条件: 接近截止时间)

**When**
1. 确认导入完成(时间边界) by 普通用户

**Then**
1. 状态转换为已完成(时间边界生效) (state)
**依赖**：PROC-003, PROC-126, PROC-127, PROC-149

### PROC-151：文件导入任务超过截止时间时，普通用户尝试确认导入完成，验证操作不可执行或状态不变
**业务定位**：时效约束 ｜ 导入任务
**覆盖需求**：T-009（4.5.3）

**Given**
1. 导入任务.任务状态 = 待执行 (时间过期条件: 超过截止时间)

**When**
1. 尝试确认导入完成(已过期) by 普通用户

**Then**
1. 操作不可执行或状态不变 (behavior)
**依赖**：PROC-003, PROC-126, PROC-127, PROC-149

### PROC-152：扫描任务待二级审批且任务级别为C级时，二级审批员审批拒绝，验证任务状态变更为审批拒绝及终止扫描
**业务定位**：分支路径 ｜ 文件扫描任务（审批拒绝）
**覆盖需求**：T-077（4.13.2）

**Given**
1. 文件扫描任务.任务状态 = 待二级审批 (扫描任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 文件扫描任务.任务状态 若任务级别=C级且二级审批员拒绝，则扫描任务终止；扫描申请人需重新提交扫描任务 (behavior)
**依赖**：PROC-007, PROC-099

### PROC-153：扫描任务待审批且任务级别为B级时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及终止扫描
**业务定位**：分支路径 ｜ 文件扫描任务（审批拒绝）
**覆盖需求**：T-078[a]（4.13.2）

**Given**
1. 文件扫描任务.任务状态 = 待审批 (扫描任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 文件扫描任务.任务状态 扫描任务终止；扫描申请人需重新提交扫描任务 (behavior)
**依赖**：PROC-007, PROC-081

### PROC-154：扫描任务待审批且任务级别为C级时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及终止扫描
**业务定位**：分支路径 ｜ 文件扫描任务（审批拒绝）
**覆盖需求**：T-078[b]（4.13.2）

**Given**
1. 文件扫描任务.任务状态 = 待审批 (扫描任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 文件扫描任务.任务状态 扫描任务终止；扫描申请人需重新提交扫描任务 (behavior)
**依赖**：PROC-007, PROC-081

### PROC-155：扫描任务待执行时，载体管理员确认扫描完成，验证任务状态变更为已完成及归档任务信息和扫描记录
**业务定位**：正向流程 ｜ 文件扫描任务（已完成）
**覆盖需求**：T-081（4.13.3）

**Given**
1. 文件扫描任务.任务状态 = 待执行
2. 文件扫描任务.任务状态 = 待执行 (载体管理员在线确认文件扫描完成)

**When**
1. 确认扫描完成 by 载体管理员

**Then**
1. 状态流转：待执行→已完成 (state)
2. 文件扫描任务.任务状态 同时归档任务信息和扫描记录 (behavior)
**依赖**：PROC-007, PROC-128, PROC-129

### PROC-156：归档任务待二级审批且任务级别为C级时，二级审批员审批拒绝，验证任务状态变更为审批拒绝及终止归档
**业务定位**：分支路径 ｜ 载体归档任务（审批拒绝）
**覆盖需求**：T-023（4.7.2）

**Given**
1. 载体归档任务.任务状态 = 待二级审批 (归档任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 载体归档任务.任务状态 若任务级别=C级且二级审批员拒绝，则归档任务终止；归档申请人需重新提交归档任务 (behavior)
**依赖**：PROC-022, PROC-100

### PROC-157：B级归档任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体归档任务（审批拒绝）
**覆盖需求**：T-024[a]（4.7.2）

**Given**
1. 载体归档任务.任务状态 = 待审批 (归档任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体归档任务.任务状态 归档任务终止；归档申请人需重新提交归档任务 (behavior)
**依赖**：PROC-022, PROC-091

### PROC-158：C级归档任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体归档任务（审批拒绝）
**覆盖需求**：T-024[b]（4.7.2）

**Given**
1. 载体归档任务.任务状态 = 待审批 (归档任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体归档任务.任务状态 归档任务终止；归档申请人需重新提交归档任务 (behavior)
**依赖**：PROC-022, PROC-091

### PROC-159：载体管理员确认归档完成，验证任务状态变更为已完成及载体状态变更为已归档
**业务定位**：正向流程 ｜ 载体归档任务（已完成）
**覆盖需求**：T-027（4.7.3）

**Given**
1. 载体归档任务.任务状态 = 待执行
2. 载体归档任务.任务状态 = 待执行 (载体管理员已接收待归档载体)

**When**
1. 确认归档完成 by 载体管理员

**Then**
1. 状态流转：待执行→已完成 (state)
2. 申请人能够看到本人名下的载体状态为已归档；对已归档载体，无法进行移交、留存、回收、外送操作 (behavior)
3. 载体归档任务执行完成后，载体状态变为已归档 (behavior)
**依赖**：PROC-022, PROC-130, PROC-131

### PROC-160：C级外送任务待二级审批时，二级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体外送任务（审批拒绝）
**覆盖需求**：T-059（4.11.2）

**Given**
1. 载体外送任务.任务状态 = 待二级审批 (外送任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 载体外送任务.任务状态 若任务级别=C级且二级审批员拒绝，则外送任务终止；外送申请人需重新提交载体外送任务 (behavior)
**依赖**：PROC-031, PROC-101

### PROC-161：B级外送任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体外送任务（审批拒绝）
**覆盖需求**：T-060[a]（4.11.2）

**Given**
1. 载体外送任务.任务状态 = 待审批 (外送任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体外送任务.任务状态 外送任务终止；外送申请人需重新提交载体外送任务 (behavior)
**依赖**：PROC-031, PROC-092

### PROC-162：C级外送任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体外送任务（审批拒绝）
**覆盖需求**：T-060[b]（4.11.2）

**Given**
1. 载体外送任务.任务状态 = 待审批 (外送任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体外送任务.任务状态 外送任务终止；外送申请人需重新提交载体外送任务 (behavior)
**依赖**：PROC-031, PROC-092

### PROC-163：外送任务待执行时，载体管理员确认外送完成，验证任务状态变更为已完成及载体状态更新
**业务定位**：正向流程 ｜ 载体外送任务（已完成）
**覆盖需求**：T-063（4.11.3）

**Given**
1. 载体外送任务.任务状态 = 待执行
2. 载体外送任务.任务状态 = 待执行 (载体管理员在线确认外送完成)

**When**
1. 确认外送完成 by 载体管理员

**Then**
1. 状态流转：待执行→已完成 (state)
2. 载体状态变为已外送；外送任务完成 (behavior)
3. 载体外送任务执行完成后，载体状态变为已外送 (behavior)
**依赖**：PROC-031, PROC-132, PROC-133

### PROC-164：C级回收任务待二级审批时，二级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体回收任务（审批拒绝）
**覆盖需求**：T-050（4.10.2）

**Given**
1. 载体回收任务.任务状态 = 待二级审批 (回收任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 载体回收任务.任务状态 若任务级别=C级且二级审批员拒绝，则回收任务终止；回收申请人需重新提交回收任务 (behavior)
**依赖**：PROC-041, PROC-102

### PROC-165：B级回收任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体回收任务（审批拒绝）
**覆盖需求**：T-051[a]（4.10.2）

**Given**
1. 载体回收任务.任务状态 = 待审批 (回收任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体回收任务.任务状态 回收任务终止；回收申请人需重新提交回收任务 (behavior)
**依赖**：PROC-041, PROC-093

### PROC-166：C级回收任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及任务终止
**业务定位**：分支路径 ｜ 载体回收任务（审批拒绝）
**覆盖需求**：T-051[b]（4.10.2）

**Given**
1. 载体回收任务.任务状态 = 待审批 (回收任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体回收任务.任务状态 回收任务终止；回收申请人需重新提交回收任务 (behavior)
**依赖**：PROC-041, PROC-093

### PROC-167：载体管理员确认回收完成，验证任务状态变更为已完成
**业务定位**：正向流程 ｜ 载体回收任务（已完成）
**覆盖需求**：T-054（4.10；4.10.3）

**Given**
1. 载体回收任务.任务状态 = 待执行
2. 载体回收任务.任务状态 = 待执行 (载体管理员已接收待回收载体)

**When**
1. 确认回收完成 by 载体管理员

**Then**
1. 状态流转：待执行→已完成 (state)
2. 载体状态变为已回收；回收任务完成 (behavior)
3. 载体回收任务执行完成后，载体状态变为已回收 (behavior)
**依赖**：PROC-041, PROC-134, PROC-135

### PROC-168：C级登记任务待二级审批时，二级审批员拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 载体登记任务（审批拒绝）
**覆盖需求**：T-014（4.6.2）

**Given**
1. 载体登记任务.任务状态 = 待二级审批 (登记任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 载体登记任务.任务状态 若任务级别=C级且二级审批员拒绝，则登记任务终止；登记申请人需重新提交登记任务 (behavior)
**依赖**：PROC-050, PROC-103

### PROC-169：B级登记任务待审批时，一级审批员拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 载体登记任务（审批拒绝）
**覆盖需求**：T-015[a]（4.6.2）

**Given**
1. 载体登记任务.任务状态 = 待审批 (登记任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体登记任务.任务状态 登记任务终止；登记申请人需重新提交登记任务 (behavior)
**依赖**：PROC-050, PROC-094

### PROC-170：C级登记任务待审批时，一级审批员拒绝，验证任务状态变更为审批拒绝
**业务定位**：分支路径 ｜ 载体登记任务（审批拒绝）
**覆盖需求**：T-015[b]（4.6.2）

**Given**
1. 载体登记任务.任务状态 = 待审批 (登记任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体登记任务.任务状态 登记任务终止；登记申请人需重新提交登记任务 (behavior)
**依赖**：PROC-050, PROC-094

### PROC-171：载体管理员完成登记执行，验证任务状态变更为已完成
**业务定位**：正向流程 ｜ 载体登记任务（已完成）
**覆盖需求**：T-018（4.6.3；4.6.5）

**Given**
1. 载体登记任务.任务状态 = 待执行
2. 载体登记任务.任务状态 = 待执行 (载体管理员已实施载体登记)

**When**
1. 完成登记执行 by 载体管理员

**Then**
1. 状态流转：待执行→已完成 (state)
2. 载体登记任务.任务状态 登记完成后，申请人能够看到本人名下的载体；载体记入申请人台账，持有时间默认72小时 (behavior)
**依赖**：PROC-050, PROC-136, PROC-137
**弱依赖**：PROC-016, PROC-017

### PROC-172：执行载体类别为纸质必填纸张页数规则
**业务定位**：业务规则 ｜ 载体登记任务
**覆盖需求**：RO-BR-018（4.6.1（1）；4.6.1（2））

**Then**
1. [BR-018] 载体类别为纸质时，纸张页数必填；数值范围1-9999 (behavior) [BR: BR-018]

### PROC-173：C级留存任务待二级审批时，二级审批拒绝，验证任务状态变更为审批拒绝并终止任务
**业务定位**：分支路径 ｜ 载体留存任务（审批拒绝）
**覆盖需求**：T-041（4.9.2）

**Given**
1. 载体留存任务.任务状态 = 待二级审批 (留存任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 载体留存任务.任务状态 若任务级别=C级且二级审批员拒绝，则留存任务终止；留存申请人需重新提交留存任务 (behavior)
**依赖**：PROC-061, PROC-104

### PROC-174：B级留存任务待审批时，一级审批拒绝，验证任务状态变更为审批拒绝并终止任务
**业务定位**：分支路径 ｜ 载体留存任务（审批拒绝）
**覆盖需求**：T-042[a]（4.9.2）

**Given**
1. 载体留存任务.任务状态 = 待审批 (留存任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体留存任务.任务状态 留存任务终止；留存申请人需重新提交留存任务 (behavior)
**依赖**：PROC-061, PROC-095

### PROC-175：C级留存任务待审批时，一级审批拒绝，验证任务状态变更为审批拒绝并终止任务
**业务定位**：分支路径 ｜ 载体留存任务（审批拒绝）
**覆盖需求**：T-042[b]（4.9.2）

**Given**
1. 载体留存任务.任务状态 = 待审批 (留存任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体留存任务.任务状态 留存任务终止；留存申请人需重新提交留存任务 (behavior)
**依赖**：PROC-061, PROC-095

### PROC-176：留存任务待执行时，系统完成执行，验证任务状态变更为已完成并处理载体
**业务定位**：正向流程 ｜ 载体留存任务（已完成）
**覆盖需求**：T-045（4.9.3）

**Given**
1. 载体留存任务.任务状态 = 待执行
2. 载体留存任务.任务状态 = 待执行 (载体持有时间已增加)

**When**
1. 完成留存执行 by 系统

**Then**
1. 状态流转：待执行→已完成 (state)
2. 载体留存任务.任务状态 载体持有时间到期前12小时系统自动向持有人发送载体到期提醒；持有人可选择将载体进行归档、移交、回收、留存或者外送；到期不处理系统自动将此载体持有人用户进行功能限制 (behavior)
**依赖**：PROC-061, PROC-138, PROC-139

### PROC-177：接近截止时间时，系统完成留存执行，验证任务状态变更为已完成
**业务定位**：时效约束 ｜ 载体留存任务（已完成）
**覆盖需求**：T-045（4.9.3）

**Given**
1. 载体留存任务.任务状态 = 待执行 (时间边界条件: 接近截止时间)

**When**
1. 完成留存执行(时间边界) by 系统

**Then**
1. 状态转换为已完成(时间边界生效) (state)
**依赖**：PROC-061, PROC-138, PROC-139, PROC-176

### PROC-178：超过截止时间时，系统尝试完成留存执行，验证操作不可执行或状态不变
**业务定位**：时效约束 ｜ 载体留存任务
**覆盖需求**：T-045（4.9.3）

**Given**
1. 载体留存任务.任务状态 = 待执行 (时间过期条件: 超过截止时间)

**When**
1. 尝试完成留存执行(已过期) by 系统

**Then**
1. 操作不可执行或状态不变 (behavior)
**依赖**：PROC-061, PROC-138, PROC-139, PROC-176

### PROC-179：留存时间规则执行时，验证留存时间只能填入1~48之间的整数；留存最长可增加48小时持有时间
**业务定位**：业务规则 ｜ 载体留存任务
**覆盖需求**：RO-BR-015（4.9.1；4.9）

**Then**
1. [BR-015] 留存时间只能填入1~48之间的整数；留存最长可增加48小时持有时间 (behavior) [BR: BR-015]

### PROC-180：C级移交任务待二级审批时，二级审批员拒绝，验证任务状态转换为审批拒绝并终止任务
**业务定位**：分支路径 ｜ 载体移交任务（审批拒绝）
**覆盖需求**：T-032（4.8.2）

**Given**
1. 载体移交任务.任务状态 = 待二级审批 (移交任务处于待二级审批状态)
2. 约束：任务级别为C级
3. 分支条件：任务级别=C级

**When**
1. 二级审批拒绝 by 二级审批员

**Then**
1. 状态流转：待二级审批→审批拒绝 (state)
2. 载体移交任务.任务状态 若任务级别=C级且二级审批员拒绝，则移交任务终止；移交申请人需重新提交移交任务 (behavior)
**依赖**：PROC-070, PROC-105

### PROC-181：B级移交任务待审批时，一级审批员拒绝，验证任务状态转换为审批拒绝并终止任务
**业务定位**：分支路径 ｜ 载体移交任务（审批拒绝）
**覆盖需求**：T-033[a]（4.8.2）

**Given**
1. 载体移交任务.任务状态 = 待审批 (移交任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=B级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体移交任务.任务状态 移交任务终止；移交申请人需重新提交移交任务 (behavior)
**依赖**：PROC-070, PROC-096

### PROC-182：C级移交任务待审批时，一级审批员审批拒绝，验证任务状态变更为审批拒绝及终止移交
**业务定位**：分支路径 ｜ 载体移交任务（审批拒绝）
**覆盖需求**：T-033[b]（4.8.2）

**Given**
1. 载体移交任务.任务状态 = 待审批 (移交任务处于待审批状态)
2. 约束：任务级别为B级或C级
3. 分支条件：任务级别=C级

**When**
1. 审批拒绝 by 一级审批员

**Then**
1. 状态流转：待审批→审批拒绝 (state)
2. 载体移交任务.任务状态 移交任务终止；移交申请人需重新提交移交任务 (behavior)
**依赖**：PROC-070, PROC-096

### PROC-183：接收人在线确认载体移交后，普通用户确认移交完成，验证任务状态变更为已完成及台账变更
**业务定位**：正向流程 ｜ 载体移交任务（已完成）
**覆盖需求**：T-036（4.8.3）

**Given**
1. 载体移交任务.任务状态 = 待执行
2. 载体移交任务.任务状态 = 待执行 (接收人在线确认载体移交)

**When**
1. 确认移交完成 by 普通用户

**Then**
1. 状态流转：待执行→已完成 (state)
2. 载体移交任务.任务状态 移交成功后，申请人台账中看不到此载体；接收人台账中自动增加此载体；系统自动记录移交历史并变更台账中的载体归属人 (behavior)
**依赖**：PROC-070, PROC-140, PROC-141

### PROC-184：新增部门后，验证可增加下级机构信息
**业务定位**：数据操作 ｜ 部门
**覆盖需求**：EO-CRU-076（4.14.3（1））

**When**
1. 导航至部门页面
2. 新增部门
   表单字段：部门名称(字符；必填项；名称唯一)；排序(数字；默认为0)；备注(字符)

**Then**
1. 点击添加下级机构按钮可增加下级机构信息 (behavior)

### PROC-185：编辑部门时，验证已存在部门信息的编辑，被客户使用的部门不能修改必填项
**业务定位**：数据操作 ｜ 部门
**覆盖需求**：EO-CRU-077（4.14.3（2））

**When**
1. 导航至部门页面
2. 编辑部门
   表单字段：部门名称(字符；必填项；名称唯一)；排序(数字；默认为0)；备注(字符)

**Then**
1. 实现对已存在部门信息的编辑，被客户使用的部门不能进行必填项信息的修改 (behavior)

### PROC-186：删除部门时，验证已存在部门信息的删除，被客户使用和实验单使用的部门不能删除
**业务定位**：数据操作 ｜ 部门
**覆盖需求**：EO-CRU-078（4.14.3（3））

**When**
1. 导航至部门页面
2. 删除部门

**Then**
1. 实现对已存在部门信息的删除，被客户使用和实验单使用的部门不能进行删除 (behavior)
2. [BR-001] 不能删除修改根部门 (behavior) [BR: BR-001]
3. [BR-026] 不能删除修改根部门；被客户使用的部门不能进行必填项信息的修改；被客户使用和实验单使用的部门不能进行删除 (behavior) [BR: BR-026]

### PROC-187：按规则执行操作事件，验证部门名称必填项及名称唯一
**业务定位**：业务规则 ｜ 部门
**覆盖需求**：RO-BR-027（4.14.3 表4.14.3-1（1））

**Then**
1. [BR-027] 部门名称必填项；名称唯一 (behavior) [BR: BR-027]

### PROC-188：提交含违规值的部门表单，验证部门名称、排序校验失败及提示信息
**业务定位**：字段校验 ｜ 部门
**覆盖需求**：FIELD-VAL-部门

**Given**
1. 部门 状态 = 可编辑 (部门信息录入页面已打开)

**When**
1. 导航至部门新增/编辑页面
2. 提交含违规值的部门表单 by 系统管理员

**Then**
1. 部门.部门名称 校验失败，提示'必填项不能为空' (behavior)
2. 校验失败，提示'部门名称已存在，请输入唯一的名称' (behavior)
3. 校验失败，提示'排序必须是数字' (behavior)

### PROC-189：新增导出申请，验证进入导出申请页面并录入申请内容
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-052（4.12.1）

**When**
1. 导航至文件导出任务页面
2. 新增导出申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；导出方式(字符；必填项；从光盘、U盘、邮件中选择；选择邮件方式需添加邮箱地址)；密码保护(字符；必填项；从是、否中选择；选择是系统对导出文件加密，收件箱收到加密密码)；文件列表(字符；必填项；从本地磁盘选择上传文件并确定文件级别；文件名必须标明文件级别；导出文件不大于2GB)；份数(字符；必填项；输入1~99数字)；联系电话(数字；可选项)；备注(字符；可选项)

**Then**
1. 进入导出申请页面，可录入申请内容 (behavior)

### PROC-190：编辑导出申请，验证对已存在的导出申请记录进行修改
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-053（4.4（2））

**When**
1. 导航至文件导出任务页面
2. 编辑导出申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；导出方式(字符；必填项；从光盘、U盘、邮件中选择；选择邮件方式需添加邮箱地址)；密码保护(字符；必填项；从是、否中选择；选择是系统对导出文件加密，收件箱收到加密密码)；文件列表(字符；必填项；从本地磁盘选择上传文件并确定文件级别；文件名必须标明文件级别；导出文件不大于2GB)；份数(字符；必填项；输入1~99数字)；联系电话(数字；可选项)；备注(字符；可选项)

**Then**
1. 可对已存在的导出申请记录进行修改 (behavior)

### PROC-191：暂存导出申请，验证保存为草稿状态
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-054（4.4）

**When**
1. 导航至文件导出任务页面
2. 暂存导出申请

**Then**
1. 保存为草稿状态 (behavior)

### PROC-192：上传导出文件，验证从本地磁盘中选择文件并确定文件级别，系统自动获取文件信息
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-055（4.12.1（2）；4.12.1（3））

**When**
1. 导航至文件导出任务页面
2. 上传导出文件

**Then**
1. 从本地磁盘中选择需要上传的文件，确定文件级别；文件名称、级别、大小由系统自动从文件中获取 (behavior)

### PROC-193：存在操作入口时，查询导出任务，验证看到本人提交的任务列表及状态
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-056（4.12.4）

**When**
1. 导航至文件导出任务页面
2. 查询导出任务

**Then**
1. 申请监控页面看到本人提交的文件导出任务列表及当前状态 (behavior)

### PROC-194：存在操作入口时，下载导出任务，验证批量下载保存功能
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-057（4.12.4）

**When**
1. 导航至文件导出任务页面
2. 下载导出任务

**Then**
1. 能够将文件导出任务批量下载保存 (behavior)

### PROC-195：存在操作入口时，查看导出任务详情，验证提供详细信息及文件快照
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-058（4.4（4）；4.12.1（4））

**When**
1. 导航至文件导出任务页面
2. 查看导出任务详情

**Then**
1. 提供记录的详细信息；审批人可查看到上传的文件快照 (behavior)

### PROC-196：存在操作入口时，删除导出申请，验证删除记录及列表刷新
**业务定位**：数据操作 ｜ 文件导出任务
**覆盖需求**：EO-CRU-059（4.4（5））

**When**
1. 导航至文件导出任务页面
2. 删除导出申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)

### PROC-197：存在规则适用前提时，执行导出规则，验证规则执行结果
**业务定位**：业务规则 ｜ 文件导出任务
**覆盖需求**：RO-BR-016（4.12.1（5）；4.12.1（2））

**Then**
1. [BR-016] 导出文件不大于2GB；份数输入1~99数字；上传文件名中必须标明文件级别，例如:导出文件(A级)；级别可以使用中英文圆括号和中括号标记 (behavior) [BR: BR-016]

### PROC-198：信息录入页面打开时，提交含违规值的表单，验证各字段校验失败提示
**业务定位**：字段校验 ｜ 文件导出任务
**覆盖需求**：FIELD-VAL-文件导出任务

**Given**
1. 文件导出任务 状态 = 可编辑 (文件导出任务信息录入页面已打开)

**When**
1. 导航至文件导出任务新增/编辑页面
2. 提交含违规值的文件导出任务表单 by 普通用户

**Then**
1. 校验失败，提示'申请事由必填，不能为空' (behavior)
2. 校验失败，提示'申请事由长度超过30字符' (behavior)
3. 校验失败，提示'导出方式必填，不能为空' (behavior)
4. 校验失败，提示'导出方式选择错误，请选择光盘、U盘或邮件' (behavior)
5. 校验失败，提示'密码保护必填，不能为空' (behavior)
6. 校验失败，提示'密码保护选择错误，请选择是或否' (behavior)
7. 校验失败，提示'文件列表必填，不能为空' (behavior)
8. 校验失败，提示'文件列表格式错误，文件名未标明文件级别' (behavior)
9. 文件导出任务.文件列表 校验失败，提示'导出文件超过2GB限制' (behavior)
10. 校验失败，提示'份数必填，不能为空' (behavior)
11. 校验失败，提示'份数不在1~99之间' (behavior)
12. 校验失败，提示'联系电话格式错误，应为数字' (behavior)

### PROC-199：存在操作入口时，新增导入申请，验证进入申请页面并录入内容
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-001（4.5.1（1））

**When**
1. 导航至导入任务页面
2. 新增导入申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成，规则为:任务类型+申请时间+流水序号)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；20个字符)；源数据单位(字符；必填项)；源文件存储介质(字符；必填项；可选:光盘、U盘、其它；选择其它时文本框必须输入内容)；任务级别(字符；必填项；只可选:A级、B级、C级)；联系电话(数字；可选项)；备注(字符；可选项)

**Then**
1. 进入导入申请页面，可录入申请内容 (behavior)

### PROC-200：存在操作入口时，编辑导入申请，验证修改已存在申请记录
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-002（4.4（2））

**When**
1. 导航至导入任务页面
2. 编辑导入申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成，规则为:任务类型+申请时间+流水序号)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；20个字符)；源数据单位(字符；必填项)；源文件存储介质(字符；必填项；可选:光盘、U盘、其它；选择其它时文本框必须输入内容)；任务级别(字符；必填项；只可选:A级、B级、C级)；联系电话(数字；可选项)；备注(字符；可选项)

**Then**
1. 可对已存在的导入申请记录进行修改 (behavior)

### PROC-201：存在操作入口时，暂存导入申请，验证保存为草稿状态
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-003（4.4）

**When**
1. 导航至导入任务页面
2. 暂存导入申请

**Then**
1. 保存为草稿状态，不进入审批流程 (behavior)

### PROC-202：存在操作入口时，查询导入任务，验证看到本人提交的任务列表及状态
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-004（4.5.4）

**When**
1. 导航至导入任务页面
2. 查询导入任务

**Then**
1. 申请监控页面看到本人提交的导入任务列表及当前状态 (behavior)

### PROC-203：下载导入文件，验证批量下载保存功能
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-005（4.5.4）

**When**
1. 导航至导入任务页面
2. 下载导入文件

**Then**
1. 能够将导入任务批量下载保存 (behavior)

### PROC-204：查看导入任务详情，验证提供记录的详细信息
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-006（4.4（4））

**When**
1. 导航至导入任务页面
2. 查看导入任务详情

**Then**
1. 提供记录的详细信息 (behavior)

### PROC-205：删除导入申请，验证业务允许情况下删除选中记录并实时刷新列表数据
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-007（4.4（5））

**When**
1. 导航至导入任务页面
2. 删除导入申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)

### PROC-206：确认导入完成，验证任务结束
**业务定位**：数据操作 ｜ 导入任务
**覆盖需求**：EO-CRU-008（4.5.3）

**When**
1. 导航至导入任务页面
2. 确认导入完成

**Then**
1. 此任务结束 (behavior)

### PROC-207：导入任务相关数据准备就绪时，执行规则操作，验证新增、编辑页面必填项标识及提示
**业务定位**：业务规则 ｜ 导入任务
**覆盖需求**：RO-BR-008（4.19（6）；4.19（7））

**Then**
1. [BR-008] 新增、编辑页面中的必填项应给出*标识，对未输入的必填项给出提示；有约束的必填项，输入不符合约束时应给出提示 (behavior) [BR: BR-008]

### PROC-208：导入任务相关数据准备就绪时，执行规则操作，验证查询功能组合查询支持模糊查询
**业务定位**：业务规则 ｜ 导入任务
**覆盖需求**：RO-BR-009（4.19（5）；4.4（6））

**Then**
1. [BR-009]正面: 查询功能中的组合查询支持一个（含）以上字段'与'的模糊查询 (behavior) [BR: BR-009]
2. [BR-009]负面: 不满足条件时规则不触发 (behavior) [BR: BR-009]

### PROC-209：导入任务相关数据准备就绪时，执行规则操作，验证操作支持可逆性处理
**业务定位**：业务规则 ｜ 导入任务
**覆盖需求**：RO-BR-010（4.19（4）；4.4（8）；4.4（9））

**Then**
1. [BR-010]正面: 对操作支持可逆性处理，有取消或者关闭操作 (behavior) [BR: BR-010]
2. [BR-010]负面: 不满足条件时规则不触发 (behavior) [BR: BR-010]

### PROC-210：导入任务相关数据准备就绪时，执行规则操作，验证导入任务执行完成后未确认自动结束及文件删除
**业务定位**：业务规则 ｜ 导入任务
**覆盖需求**：RO-BR-012（4.5.3）

**Then**
1. [BR-012]正面: 导入任务执行完成后如未进行确认，系统会在48小时后结束此任务，导入文件自动从系统中删除 (behavior) [BR: BR-012]
2. [BR-012]负面: 不满足条件时规则不触发 (behavior) [BR: BR-012]

### PROC-211：导入任务相关数据准备就绪时，执行规则操作，验证流水号系统自动生成规则
**业务定位**：业务规则 ｜ 导入任务
**覆盖需求**：RO-BR-038（4.5.1 表4.5-1（3）；4.6.1；4.7.1；4.8.1；4.9.1；4.10.1；4.11.1；4.12.1；4.13.1）

**Then**
1. [BR-038] 流水号系统自动生成，规则为:任务类型+申请时间+流水序号 (behavior) [BR: BR-038]

### PROC-212：导入任务相关数据准备就绪时，执行规则操作，验证申请人、申请部门自动获取
**业务定位**：业务规则 ｜ 导入任务
**覆盖需求**：RO-BR-039（4.5.1 表4.5-1（1）（2）；4.6.1；4.7.1；4.8.1；4.9.1；4.10.1；4.11.1；4.12.1；4.13.1）

**Then**
1. [BR-039] 申请人、申请部门根据登录用户自动获取 (behavior) [BR: BR-039]

### PROC-213：导入任务信息录入页面打开时，提交含违规值的表单，验证各项校验失败提示
**业务定位**：字段校验 ｜ 导入任务
**覆盖需求**：FIELD-VAL-导入任务

**Given**
1. 导入任务 状态 = 可编辑 (导入任务信息录入页面已打开)

**When**
1. 导航至导入任务新增/编辑页面
2. 提交含违规值的导入任务表单 by 普通用户

**Then**
1. 导入任务.申请事由 校验失败，提示'最长20字符' (behavior)
2. 导入任务.源数据单位 校验失败，提示'必填项不能为空' (behavior)
3. 导入任务.任务级别 校验失败，提示'必选项不能为空，请选择A级、B级或C级' (behavior)
4. 导入任务.联系电话 校验失败，提示'输入格式错误，请输入正确的数字' (behavior)
5. 导入任务.备注 校验失败，提示'可选项，无需输入' (behavior)

### PROC-214：查询角色，验证通过角色名称字段查询
**业务定位**：数据操作 ｜ 角色
**覆盖需求**：EO-CRU-079（4.14.2（1））

**When**
1. 导航至角色页面
2. 查询角色

**Then**
1. 通过角色名称字段进行查询 (behavior)

### PROC-215：查看角色下用户，验证查看对应用户信息
**业务定位**：数据操作 ｜ 角色
**覆盖需求**：EO-CRU-080（4.14.2（2））

**When**
1. 导航至角色页面
2. 查看角色下用户

**Then**
1. 查看该角色下对应的用户信息 (behavior)

### PROC-216：尝试违反规则操作，验证内置角色不可更改、删除
**业务定位**：非法验证 ｜ 角色
**覆盖需求**：RO-BR-032（5；5）

**When**
1. 尝试违反规则操作事件

**Then**
1. 本样品内置3个角色（系统管理员、监督员、日志管理员），不可对其进行更改、删除；系统管理员角色绑定系统管理员用户(sysadmin_a)、监督员(secadmin_a)和日志管理员(secauditor_a)，不可更改、删除和禁用，也不授予其他用户 (prompt) [BR: BR-032]
2. [BR-032] 本样品内置3个角色（系统管理员、监督员、日志管理员），不可对其进行更改、删除；系统管理员角色绑定系统管理员用户(sysadmin_a)、监督员(secadmin_a)和日志管理员(secauditor_a)，不可更改、删除和禁用，也不授予其他用户 (behavior) [BR: BR-032] [cross: 用户]

### PROC-217：执行角色操作事件，验证角色管理员可授权和删除规则
**业务定位**：业务规则 ｜ 角色
**覆盖需求**：RO-BR-031（4.16.1；4.16.2；4.16.3）

**Then**
1. [BR-031] 角色管理员可授权普通用户、一级审批员、二级审批员、载体管理员和监督员；删除角色后用户角色默认为普通用户 (behavior) [BR: BR-031]

### PROC-218：执行基于角色访问控制规则，验证正面和负面规则触发情况
**业务定位**：业务规则 ｜ 角色
**覆盖需求**：RO-BR-036（4.18（1））

**Then**
1. [BR-036]正面: 实现基于角色的访问控制 (behavior) [BR: BR-036]
2. [BR-036]负面: 不满足条件时规则不触发 (behavior) [BR: BR-036]

### PROC-219：操作入口可用时，新增扫描申请，验证进入扫描申请页面并录入申请内容
**业务定位**：数据操作 ｜ 文件扫描任务
**覆盖需求**：EO-CRU-060（4.13.1）

**When**
1. 导航至文件扫描任务页面
2. 新增扫描申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；文件级别(字符；必填项；只可选:A级、B级、C级)；文件页数(字符；必填项；只能填入1~100之间的数字)；联系电话(数字；可选项)；扫描内容(字符；必选项)

**Then**
1. 进入扫描申请页面，可录入申请内容 (behavior)

### PROC-220：操作入口可用时，编辑扫描申请，验证修改已存在的扫描申请记录
**业务定位**：数据操作 ｜ 文件扫描任务
**覆盖需求**：EO-CRU-061（4.4（2））

**When**
1. 导航至文件扫描任务页面
2. 编辑扫描申请
   表单字段：申请人(字符；必填项；根据登录用户自动获取申请人姓名)；申请部门(字符；必填项；根据登录用户自动获取申请人部门)；流水号(字符；必填项；系统自动生成)；申请时间(字符；必填项；系统自动生成)；申请事由(字符；必填项；30个字符)；文件级别(字符；必填项；只可选:A级、B级、C级)；文件页数(字符；必填项；只能填入1~100之间的数字)；联系电话(数字；可选项)；扫描内容(字符；必选项)

**Then**
1. 可对已存在的扫描申请记录进行修改 (behavior)

### PROC-221：暂存扫描申请，验证保存为草稿状态
**业务定位**：数据操作 ｜ 文件扫描任务
**覆盖需求**：EO-CRU-062（4.4）

**When**
1. 导航至文件扫描任务页面
2. 暂存扫描申请

**Then**
1. 保存为草稿状态 (behavior)

### PROC-222：查询扫描任务，验证申请监控页面显示任务列表及状态
**业务定位**：数据操作 ｜ 文件扫描任务
**覆盖需求**：EO-CRU-063（4.13.4）

**When**
1. 导航至文件扫描任务页面
2. 查询扫描任务

**Then**
1. 申请监控页面看到本人提交的文件扫描任务列表及当前状态 (behavior)

### PROC-223：下载扫描任务，验证批量下载保存任务
**业务定位**：数据操作 ｜ 文件扫描任务
**覆盖需求**：EO-CRU-064（4.13.4）

**When**
1. 导航至文件扫描任务页面
2. 下载扫描任务

**Then**
1. 能够将文件扫描任务批量下载保存 (behavior)

### PROC-224：查看扫描任务详情，验证提供记录的详细信息
**业务定位**：数据操作 ｜ 文件扫描任务
**覆盖需求**：EO-CRU-065（4.4（4））

**When**
1. 导航至文件扫描任务页面
2. 查看扫描任务详情

**Then**
1. 提供记录的详细信息 (behavior)

### PROC-225：存在操作入口时，删除扫描申请，验证业务允许删除并实时刷新列表数据
**业务定位**：数据操作 ｜ 文件扫描任务
**覆盖需求**：EO-CRU-066（4.4（5））

**When**
1. 导航至文件扫描任务页面
2. 删除扫描申请

**Then**
1. 业务允许情况下删除选中记录，列表数据实时刷新 (behavior)

### PROC-226：执行文件页数规则，验证文件页数只能填入1~100之间的数字
**业务定位**：业务规则 ｜ 文件扫描任务
**覆盖需求**：RO-BR-017（4.13.1（2））

**Then**
1. [BR-017] 文件页数只能填入1~100之间的数字 (behavior) [BR: BR-017]

### PROC-227：信息录入页面打开时，提交含违规值的文件扫描任务表单，验证多个字段校验失败并提示
**业务定位**：字段校验 ｜ 文件扫描任务
**覆盖需求**：FIELD-VAL-文件扫描任务

**Given**
1. 文件扫描任务 状态 = 可编辑 (文件扫描任务信息录入页面已打开)

**When**
1. 导航至文件扫描任务新增/编辑页面
2. 提交含违规值的文件扫描任务表单 by 普通用户

**Then**
1. 校验失败，提示'申请事由必填，不能为空' (behavior)
2. 校验失败，提示'申请事由长度超过30字符' (behavior)
3. 校验失败，提示'文件级别必填，不能为空' (behavior)
4. 校验失败，提示'文件级别选择错误，请选择A级、B级或C级' (behavior)
5. 校验失败，提示'文件页数必填，不能为空' (behavior)
6. 校验失败，提示'文件页数不在1~100之间' (behavior)
7. 校验失败，提示'联系电话格式错误，应为数字' (behavior)
8. 校验失败，提示'扫描内容必填，不能为空' (behavior)

### PROC-228：操作入口可用时，新增用户，验证用户新增功能实现
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-068（4.14.1（1））

**When**
1. 导航至用户页面
2. 新增用户
   表单字段：用户账号(字符；必填项；可以由汉字、小写字母、数字、下划线(_)组成；不可重复；不可修改)；用户姓名(字符；必填项)；登录密码(数字；必填项；由数字、大小写字母和特殊符号组成；长度大于等于8个字符小于等于18个字符)；确认密码(数字；必填项；与登录密码一致)；手机号码(数字；必填项，唯一；第一位数字1，第二位数字:3、4、5、6、7、8、9，一共11位数字)；邮箱(字符；必须包含@和.；不能包含中文、其它特殊符号)；生日(日期；yyyy-mm-dd)

**Then**
1. 实现用户新增的功能 (behavior)

### PROC-229：操作入口可用时，编辑用户，验证用户信息修改功能实现且用户账号不可修改
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-069（4.14.1（2））

**When**
1. 导航至用户页面
2. 编辑用户
   表单字段：用户账号(字符；必填项；可以由汉字、小写字母、数字、下划线(_)组成；不可重复；不可修改)；用户姓名(字符；必填项)；登录密码(数字；必填项；由数字、大小写字母和特殊符号组成；长度大于等于8个字符小于等于18个字符)；确认密码(数字；必填项；与登录密码一致)；手机号码(数字；必填项，唯一；第一位数字1，第二位数字:3、4、5、6、7、8、9，一共11位数字)；邮箱(字符；必须包含@和.；不能包含中文、其它特殊符号)；生日(日期；yyyy-mm-dd)

**Then**
1. 实现用户信息的修改，用户账号不能进行修改 (behavior)

### PROC-230：查看用户详情，验证查看用户详细信息功能实现
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-070（4.14.1（3））

**When**
1. 导航至用户页面
2. 查看用户详情

**Then**
1. 查看用户的详细信息 (behavior)

### PROC-231：重置用户密码，验证重置后的密码符合要求
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-071（4.14.1（6））

**When**
1. 导航至用户页面
2. 重置用户密码

**Then**
1. 重置后的密码为:Abcd123456！ (behavior)

### PROC-232：删除用户，验证用户删除功能实现及流程信息用户不能删除
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-072（4.14.1（7））

**When**
1. 导航至用户页面
2. 删除用户

**Then**
1. 将用户从系统中删除，具有流程信息的用户不能进行删除 (behavior)
2. [BR-025] 具有流程信息的用户不能进行删除 (behavior) [BR: BR-025]

### PROC-233：查询用户，验证根据用户账号查询用户功能实现
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-073（4.14.1（8））

**When**
1. 导航至用户页面
2. 查询用户

**Then**
1. 根据用户账号查询符合条件的用户 (behavior)

### PROC-234：用户登录，验证登录成功后显示用户名称及角色进入相应页面，并处理锁定用户登录
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-074（4.3.1（1）；4.3.1（5）；4.3.1（6））

**When**
1. 导航至用户页面
2. 登录

**Then**
1. 登录成功后进入的功能操作页面右上角显示登录人员的名称；根据角色进入相应页面 (behavior)
2. [BR-003]正面: 状态为'锁定'或'不存在'的用户不允许登录系统并给出对应提示信息 (behavior) [BR: BR-003]
3. [BR-003]负面: 不满足条件时规则不触发 (behavior) [BR: BR-003]

### PROC-235：用户存在且操作入口可用时，注销后退出登录状态并返回登录界面
**业务定位**：数据操作 ｜ 用户
**覆盖需求**：EO-CRU-075（4.3.2（1））

**When**
1. 导航至用户页面
2. 注销

**Then**
1. 退出登录状态，关闭所有操作界面，返回到用户登录界面 (behavior)

### PROC-236：用户处于锁定状态时，尝试锁定操作被拒绝且状态不变
**业务定位**：非法验证 ｜ 用户
**覆盖需求**：RO-IT-004（4.3.1（3）；4.14.1（5））

**Given**
1. 用户 状态 = 锁定 (用户处于"锁定"状态)

**When**
1. 尝试执行从"锁定"到"正常"的操作

**Then**
1. 操作被拒绝，锁定用户不可直接登录恢复正常，须由系统管理员执行解锁操作 (prompt)
2. 状态仍为锁定 (state)

### PROC-237：尝试违反规则操作验证用户账号规则
**业务定位**：非法验证 ｜ 用户
**覆盖需求**：RO-BR-019（4.14.1 表4.14.1-1（1）；4.14.1（2））

**When**
1. 尝试违反规则操作事件

**Then**
1. 用户账号可以由汉字、小写字母、数字、下划线(_)组成；不可重复；不可修改 (prompt) [BR: BR-019]
2. [BR-019] 用户账号可以由汉字、小写字母、数字、下划线(_)组成；不可重复；不可修改 (behavior) [BR: BR-019]

### PROC-238：执行连续输错3次密码锁定规则
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-002（4.3.1（2））

**Then**
1. [BR-002]正面: 用户连续输错3次密码，系统自动将此用户锁定 (behavior) [BR: BR-002]
2. [BR-002]负面: 不满足条件时规则不触发 (behavior) [BR: BR-002]

### PROC-239：执行密码输入错误提示规则
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-004（4.3.1（4））

**Then**
1. [BR-004]正面: 用户密码输入错误，经系统认证后应返回'用户名或密码错误'提示 (behavior) [BR: BR-004]
2. [BR-004]负面: 不满足条件时规则不触发 (behavior) [BR: BR-004]

### PROC-240：执行用户登录后无操作自动退出登录规则
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-005（4.3.1（7））

**Then**
1. [BR-005]正面: 用户登录后无操作30分钟，系统自动退出登录 (behavior) [BR: BR-005]
2. [BR-005]负面: 不满足条件时规则不触发 (behavior) [BR: BR-005]

### PROC-241：执行用户登录成功后显示名称和角色规则
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-006（4.3.1（5）；4.3.1（6））

**Then**
1. [BR-006]正面: 用户登录成功后，应在进入的功能操作页面右上角显示登录人员的名称；根据登录人员被指定的角色进入相应的页面，主菜单/功能操作、子菜单依据角色的不同而显示不同 (behavior) [BR: BR-006]
2. [BR-006]负面: 不满足条件时规则不触发 (behavior) [BR: BR-006]

### PROC-242：执行退出系统确认信息规则
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-007（4.3.2（2））

**Then**
1. [BR-007] 退出系统时应给出确认信息，只有确定后才可以退出，否则不能退出系统，保持当前操作页面和用户状态 (behavior) [BR: BR-007]

### PROC-243：执行登录密码规则
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-020（4.14.1 表4.14.1-1（3）；4.18（3））

**Then**
1. [BR-020] 登录密码由数字、大小写字母和特殊符号组成；长度大于等于8个字符小于等于18个字符；密码设置时长度在8到18位，要包含大小写字母、数字和特殊字符 (behavior) [BR: BR-020]

### PROC-244：执行手机号码规则
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-021（4.14.1 表4.14.1-1（5））

**Then**
1. [BR-021] 手机号码第一位数字1，第二位数字:3、4、5、6、7、8、9，一共11位数字；唯一 (behavior) [BR: BR-021]

### PROC-245：邮箱输入时，验证邮箱格式是否包含@和.，不包含中文和特殊符号
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-022（4.14.1 表4.14.1-1（6））

**Then**
1. [BR-022] 邮箱必须包含@和.；不能包含中文、其它特殊符号 (behavior) [BR: BR-022]

### PROC-246：修改密码时，验证原密码输入、新密码与原密码不同、新密码确认的正确性
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-023（4.18（4）；4.18（5）；4.18（6））

**Then**
1. [BR-023]正面: 修改密码需要输入原密码，如果输入的原密码不对，则给出相应提示；新密码与原密码应不同，否则系统给出提示；新密码需要确认，输入两次且相同，否则系统给出提示 (behavior) [BR: BR-023]
2. [BR-023]负面: 不满足条件时规则不触发 (behavior) [BR: BR-023]

### PROC-247：密码输入时，验证密码不以明文显示
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-024（4.18（2））

**Then**
1. [BR-024]正面: 密码不以明文显示 (behavior) [BR: BR-024]
2. [BR-024]负面: 不满足条件时规则不触发 (behavior) [BR: BR-024]

### PROC-248：提示信息输入时，验证提示信息不包含系统运行、技术架构等信息
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-033（4.18（9））

**Then**
1. [BR-033] 提示信息不能含有系统运行、技术架构等信息 (behavior) [BR: BR-033]

### PROC-249：登录时，验证用户账号或密码错误时的提示信息及用户不存在时的验证
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-034（4.19（1）；4.19（3））

**Then**
1. [BR-034]正面: 对用户账号或者密码错误的输入有提示；用户不存在时，登录系统时可被证实 (behavior) [BR: BR-034]
2. [BR-034]负面: 不满足条件时规则不触发 (behavior) [BR: BR-034]

### PROC-250：日期类数据输入时，验证是否提供日历选择功能
**业务定位**：业务规则 ｜ 用户
**覆盖需求**：RO-BR-035（4.19（2））

**Then**
1. [BR-035]正面: 日期类数据输入，应提供日历选择功能 (behavior) [BR: BR-035]
2. [BR-035]负面: 不满足条件时规则不触发 (behavior) [BR: BR-035]

### PROC-251：提交用户表单时，验证各字段输入的合规性
**业务定位**：字段校验 ｜ 用户
**覆盖需求**：FIELD-VAL-用户

**Given**
1. 用户 状态 = 可编辑 (用户信息录入页面已打开)

**When**
1. 导航至用户新增/编辑页面
2. 提交含违规值的用户表单 by 系统管理员

**Then**
1. 校验失败，提示'用户账号已存在，请输入唯一值' (behavior)
2. 校验失败，提示'用户账号格式错误，不可包含特殊字符' (behavior)
3. 用户.用户姓名 校验失败，提示'必填项不能为空' (behavior)
4. 校验失败，提示'用户姓名不能包含特殊字符' (behavior)
5. 用户.登录密码 校验失败，提示'密码长度必须大于等于8个字符且小于等于18个字符' (behavior)
6. 用户.登录密码 校验失败，提示'密码必须由数字、大小写字母和特殊符号组成' (behavior)
7. 校验失败，提示'确认密码与登录密码不一致' (behavior)
8. 校验失败，提示'手机号码必须是11位数字' (behavior)
9. 校验失败，提示'手机号码第一位必须是1，第二位必须是3-9' (behavior)
10. 校验失败，提示'邮箱必须包含@和.' (behavior)
11. 校验失败，提示'邮箱不能包含中文和特殊符号' (behavior)
12. 校验失败，提示'生日格式不正确，正确格式为yyyy-mm-dd' (behavior)

### PROC-252：查询登录日志时，验证根据操作人账号、名称、创建时间查询日志信息的功能
**业务定位**：数据操作 ｜ 日志
**覆盖需求**：EO-CRU-081（4.17.1（1））

**When**
1. 导航至日志页面
2. 查询登录日志

**Then**
1. 可以根据操作人账号、操作人名称、创建时间查询登录日志信息 (behavior)

### PROC-253：查询操作日志时，验证根据操作人账号、名称、创建时间查询日志信息的功能
**业务定位**：数据操作 ｜ 日志
**覆盖需求**：EO-CRU-082（4.17.1（1））

**When**
1. 导航至日志页面
2. 查询操作日志

**Then**
1. 可以根据操作人账号、操作人名称、创建时间查询操作日志信息 (behavior)

### PROC-254：导入日志文件时，验证系统进入日志导入页面并完成日志导入操作
**业务定位**：数据操作 ｜ 日志
**覆盖需求**：EO-CRU-083（4.17.1（2））

**When**
1. 导航至日志页面
2. 导入日志文件

**Then**
1. 系统进入日志导入页面，可选择需要导入的日志文件，完成日志导入操作 (behavior)

### PROC-255：导出日志Excel时，验证根据起始时间导出Excel文件并可选择删除导出项
**业务定位**：数据操作 ｜ 日志
**覆盖需求**：EO-CRU-084（4.17.2（1））

**When**
1. 导航至日志页面
2. 导出日志Excel

**Then**
1. 根据起始时间，完成导出成Excel文件操作；可选择导出后删除导出项 (behavior)

### PROC-256：日志管理时，验证日志管理只对日志管理员和角色管理员开放，日志记录只能由他们查看
**业务定位**：业务规则 ｜ 日志
**覆盖需求**：RO-BR-028（4.17；4.18（7））

**Then**
1. [BR-028]正面: 系统的日志管理只对日志管理员和角色管理员开放；系统日志记录只能由日志管理员和角色管理员查看 (behavior) [BR: BR-028]
2. [BR-028]负面: 不满足条件时规则不触发 (behavior) [BR: BR-028]

### PROC-257：日志记录时，验证登录日志、操作日志和系统日志的记录内容
**业务定位**：业务规则 ｜ 日志
**覆盖需求**：RO-BR-029（4.17.1；4.18（8））

**Then**
1. [BR-029]正面: 登录日志至少记录用户登录日志；操作日志至少记录用户信息的新增、编辑、删除；系统日志可以记录事件的时间、操作人和日志类型等 (behavior) [BR: BR-029]
2. [BR-029]负面: 不满足条件时规则不触发 (behavior) [BR: BR-029]

### PROC-258：日志导出时，验证根据起始时间完成导出成Excel文件操作，且可选择导出后删除导出项
**业务定位**：业务规则 ｜ 日志
**覆盖需求**：RO-BR-030（4.17.2（1））

**Then**
1. [BR-030]正面: 日志导出根据起始时间完成导出成Excel文件操作；可选择导出后删除导出项 (behavior) [BR: BR-030]
2. [BR-030]负面: 不满足条件时规则不触发 (behavior) [BR: BR-030]
