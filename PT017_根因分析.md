用户要求重新编辑和修改要改动的部分，输出完整代码。这是 Agent Mode 的任务——生成完整的代码文件。需要把 F1~F8 修正项整合进完整代码交付：

prompt.md — 已交付过（含轮次型判定三问 + within_rounds 语法）
model.py — 需要输出完整版：kind/progression/scoped_to 透传 + within_rounds 落盘 + P1 assemble 完备性 fail-fast 校验（F1、F6）
schema.py — 完整版：within_rounds 字段注册
struct_srs.py — 完整版：项目阶段 kind="rounds" + 12 个转换 within_rounds + 4.8.1(7) 释放 BR + 4.9.3 打分进度 op/属性 + t07 er 去阶段语句 + t41 precondition 修正（F5、F7）
generate_obligation_model.py 的补丁脚本 — 完整版：字母后缀、state_info kind 透传、rounds_split 独立 judgment（F2、F3、F4）
s4_multi_instance.py 的补丁脚本 — 完整版
DECISIONS.md 条目（F8）

这个任务应该派发给 agent_v2_subagent 执行，因为是完整的文件生成任务。但用户说"输出完整代码"——可能希望直接在对话中看到代码。考虑到文件很大（struct_srs.py 全文 30852 tokens，model.py 6164 tokens，schema.py 3994 tokens），在对话里输出全部文件会非常长。
按照系统指示，生成完整文件的任务应该走 Agent Mode（agent_v2_subagent）。之前几轮也是这样做的（browser_task_tool 调用）。但当前工具列表里是 agent_v2_subagent。我应该调用 agent_v2_subagent 来完成这个任务。
让我构建任务描述，包含全部规格：
F1: model.py add_trans 输出 dict 必须包含 within_rounds 键
F2: 变体后缀用字母 T-002[a]/T-002[b]（沿用 suffixes = "abcdefghijklmnop"）
F3: state_info 构造透传 kind（P2 Step 5 段）+ S4 补丁按 kind 排除
F4: rounds 展开的 judgment/note 独立标记（category="rounds_split"）
F5: struct_srs.py 补 4.8.1(7) 释放 BR（绑 t27/t28/t13）+ 4.9.3 打分进度（E-PSJH 属性+op）
F6: model.py assemble 完备性 fail-fast：rounds 实体的 scoped 维度全部转换必须声明 within_rounds，轮次机器自身转换禁带
F7: t07 expected_results 删"阶段由开题变为验收"语句；t41 precondition 修正（文本与 ref 匹配：开题轮+归档结束）
F8: DECISIONS.md 决策条目
需要输出的完整文件：

srs_pipeline/prompt.md（完整修改版）
srs_pipeline/model.py（完整）
srs_pipeline/schema.py（完整）
srs_data/struct_srs.py（完整，30852 tokens —— 非常大）
patch_a2_rounds.py（generate_obligation_model.py 补丁，完整）
patch_a2_s4_multi_instance.py（完整）
DECISIONS.md 追加条目（片段）

关键设计细节需要在任务描述中给全：
model.py 规格：


add_entity: dims 构造透传 kind/progression/scoped_to，缺省 kind="lifecycle"


add_trans: 新增 within_rounds=None 可选参数；输出 dict 含 "within_rounds": list(within_rounds) if within_rounds else None（或空列表？考虑空值规范——字段必须存在，无内容传 []。所以应该是 list(within_rounds or [])）


assemble 或 _assign_ids 后的校验：

kind="rounds" 时 progression 必填且与 states 一致（顺序）
within_rounds 值 ⊆ scoped rounds 维度的 progression/states
rounds 实体上 scoped 维度（如"项目状态"）的全部转换必须声明 within_rounds（除 from=None 创建转换？不对——t01 是创建转换，t40 也是。冻结方案说"scoped 维度全部转换必须声明 within_rounds"。t01 新增项目应该是双轮次吗？t01 是创建转换（None→待选入），创建时阶段初始化为开题，它不发生在某个轮次里，而是设立轮次。但 t01 在项目状态维度上，项目状态是 scoped_to="项目状态"。嗯，这里有个细节：创建转换 t01 是否需要声明？阶段上 t40 是轮次机器的创建边。t01 声明 within_rounds 意味着展开成两条变体（开题轮创建、验收轮创建）——但验收轮的项目创建不存在（新项目总是开题）。所以 t01 不应声明 within_rounds，或应豁免。

回顾冻结方案："rounds 实体上所有 scoped 维度转换一律声明 within_rounds（t07/t08=［开题］、t09/t10=［验收］、t03/t11/t11b=双轮次）。展开为单轮次时退化为自身，无需豁免"。当时的清单是 t02/t03/t04/t05/t06/t07/t08/t09/t10/t11/t11b——不含 t01。而交付报告说"t02/t03/t04/t05/t06/t07/t08/t09/t10/t11/t11b 加 within_rounds 声明"（12个？交付报告说"12 个转换加 within_rounds"——之前我审查时说七条双轮次（t02/t03/t04/t05/t06/t11/t11b）+ 单轮次（t07/t08=开题、t09/t10=验收）= 11 条。加上 t01 就是 12。但 t01 创建转换不应拆分——创建项目只在开题轮发生。
交付报告写的是"t02/t03/t04/t05/t06/t07/t08/t09/t10/t11/t11b"——数一下：t02,t03,t04,t05,t06,t07,t08,t09,t10,t11,t11b = 11 条。但报告又说 12 个。不管怎样，正确设计是 11 条（不含 t01）。创建转换豁免：fail-fast 校验应排除 from=None 的创建转换（t01），因为创建发生在轮次机制建立之前。
校验规则最终形态：

对每个 kind="rounds" 维度 R（entity E, scoped_to D）：

E 上 dimension=D 的所有非创建转换（from != None）必须声明 within_rounds 且非空 → 否则 fail-fast（CriticalAmbiguity）
E 上 dimension=R（轮次机器自身）的转换（t40/t41）不得带 within_rounds
within_rounds 值必须 ⊆ R.states（或 progression）
kind="rounds" 时 progression 必填，且 progression 与 states 集合一致（顺序可以不同，但集合相同）





_build_trace 增加 rounds 统计（可选）


schema.py 规格：

OBJECT_SCHEMA["trans"] 增加 Field("within_rounds", "within_rounds", LLM, desc="轮次限定声明：值 ∈ 所属实体 rounds 维度的 states；scoped 维度的非创建转换必填")
entity.state_dimensions 的 desc 更新：嵌套 [{dimension_name,states,initial,terminal,inferred?,note?,kind?,progression?,scoped_to?}]

注意：validate_llm 对未知参数 fail-fast——add_entity 传 dims 时，dims 是嵌套结构，validate_llm 只校验顶层参数（state_dimensions 作为一个参数），嵌套键不会被逐键校验（从 model.py 看，validate_llm("entity", {...}) 只检查顶层 dsl 字段；dims 内部在 add_entity 里手动构造）。所以 state_dimensions 嵌套键 kind/progression/scoped_to 不会触发未知参数错误——只要 add_entity 的 dims 构造代码透传它们即可。但 schema.py 的注册表仍应更新 desc 以保持文档一致性。而 add_trans 的 within_rounds 是顶层参数，必须在 OBJECT_SCHEMA["trans"] 注册，否则 validate_llm 报"未知参数"。
struct_srs.py 规格（完整数据文件，基于已读取的全文 2679 行）：


E-XM 项目阶段维度：加 kind="rounds", progression=["开题","验收"], scoped_to="项目状态"


t02: within_rounds=["开题","验收"]


t03: within_rounds=["开题","验收"]


t04: within_rounds=["开题","验收"]


t05: within_rounds=["开题","验收"]


t06: within_rounds=["开题","验收"]


t07: within_rounds=["开题"]，expected_results 改为 ["若项目归档分支=开题_合格及以上，则项目状态由待归档变为已选入"]（删"阶段由开题变为验收"）


t08: within_rounds=["开题"]


t09: within_rounds=["验收"]


t10: within_rounds=["验收"]


t11: within_rounds=["开题","验收"]


t11b: within_rounds=["开题","验收"]


t41: precondition 修正——原文本"项目归档分支为开题_合格及以上" ref 指向 E-PSJH.结束。修正为两条前置：计划结束的 state_ref（保持）+ 项目阶段=开题的 state_ref？其实 t41 是轮次机器自身的边（开题→验收），precondition 应该是：评审计划结束（E-PSJH.结束）+ 项目处于开题阶段（E-XM.项目阶段=开题）+ 开题评级合格及以上。原文本写的是"项目归档分支为开题_合格及以上"但 ref 指向 E-PSJH.结束——ref 和文本不匹配。修正方案：

precond(text="评审计划状态由已完成变为结束或超时结束", state_ref, ref=E-PSJH.结束)
precond(text="项目处于开题阶段", state_ref, ref=state_ref("E-XM","项目阶段","开题"))
precond(text="开题评级在合格及以上", constraint, note 降级理由)



阶段 2 修复：

4.8.1(7) 专家时段释放：新增 BR（b69）："只要计划结束，评审专家与计划相应的评审时段为可用"，entities_involved=["E-PSJH","E-ZJ"]，constrained_entity="E-ZJ"（或 E-PSJH？释放的是专家的时段，受影响的是专家。原文"只要计划结束，评审专家与计划相应的评审时段为可用"——这是描述性/授权性规则。signal_type：无"必须/不得"，是可用性描述……旧版 RO-BR-011 怎么建模的？"只要计划结束，评审专家与计划相应的评审时段为可用"。signal_type 取 restrictive（"只要…就…"条件句）或 usability。category=timing 或 validation。我选 signal_type="restrictive"（只要=仅当语义的条件规则），category="timing"（时段释放），source_ref="4.8.1（7）"。绑定转换：作为 BR 不直接绑转换，但 note 可注 evidence。旧版是 RO-BR-011 + PROC-229。我们加 b69 即可，S1 会生成 Type7 用例。
4.9.3 打分进度：E-PSJH 加属性 attr(name="打分进度", desc="打分已提交专家人数占评审专家总人数的比例")，加 op(name="查看打分进度", category="query", expected_results=["显示打分已提交专家人数占评审专家总人数的比例"], source_ref="4.9.3（1）", note=N(role=["评审管理员","评审助理"], comment="通用操作"))。还有"打分情况"——已有"查看评审计划执行信息"op（source_ref="4.8-1 #14"）。4.9.3 的(2)打分情况也该引用 4.9.3。但最小改动：加打分进度即可，打分情况已有承载（虽然 source_ref 是 4.8-1 #14，可改为 "4.8-1 #14；4.9.3（2）"）。这个可以顺带改。
permission：评审管理员/评审助理的 add_permission 已含"查看实施情况"——保持。



头部注释更新：t40-t41 描述、b69


patch_a2_rounds.py 规格（对 generate_obligation_model.py 的手术补丁）：


插入点：Step 2 处理循环内，matched_dims = get_matched_dims(t) 之后


逻辑：

从 p1 转换读 t.get("within_rounds")；为空 → 跳过（不加虚拟维度）
查找实体 E 的 rounds 维度：遍历 p1["domain_model"]["entities"]，找 e["id"]==t["entity"] 的 state_dimensions 中 d.get("kind")=="rounds" 的维度 → 得到 (dimension_name, states)
构造虚拟 branch_dimension 对象 fake_bd = {"dimension": rounds_dim_name, "values": within_rounds 值列表, "branches": [{"value": v, "target_transition": t["id"], "desc": f"轮次={v}"} for v in within_rounds], "coverage": {}}
matched_dims.append(fake_bd) → 走既有笛卡尔积管线
若 len(within_rounds)==1：注入预条件但仍不拆分（单组合路径）——既有代码 len(combos)==1 分支会保留原 id 并注 branch_path。但注意：单值 rounds 声明（t07/t08/t09/t10）已经带了阶段 state_ref 前置条件（原文就有"项目处于开题阶段"），如果再注入一条"项目阶段=开题"会重复。而且——t07/t10 还被 bd01（项目归档分支）匹配！get_matched_dims(t) 会返回 bd01，加上 fake_bd 后 dim_values_lists 有两个维度，笛卡尔积 4×1 或 4×2 = 4 或 8 个组合，R5 过滤后……这会出问题！

等等，这是个重要的技术细节。t07 已在 bd01 的 coverage.transitions 里（通过 note.branch_dimension 回填），matched_dims 会包含 bd01（4个值）。如果再附加 rounds 虚拟维度（开题），组合 = 4 × 1 = 4 个，其中 R5 会过滤掉不匹配的：combo_conflicts_with_preconds 检查 precond 里 "项目处于开题阶段" state_ref（dimension=项目阶段，state=开题）——bd01 的值是"开题_合格及以上"等，不冲突（不同维度名）。但 rounds 虚拟维度名="项目阶段"，precond ref.dimension=="项目阶段" state=="开题"，combo 值若是"开题"→ 不冲突；若 within_rounds=["开题"] 只有一个值，无冲突。所以 t07 会拆成 4 个分支变体（bd01 的 4 值）——这改变了现状！现状 t07 走 len(combos)==1？不对——现状 t07 matched_dims=[bd01]，dim_values_lists=[(bd01, 4 values)]，combos=4 个，R5 过滤：precond "项目评级在合格及以上"（constraint 文本，非 state_ref）——combo 值"开题_不合格或差"与文本冲突检查：dim_name="项目归档分支" 在文本中不出现（文本是"项目评级在合格及以上"），所以不过滤。combos=4 → 多组合拆分路径！t07 现状就已经被拆成 4 个变体了？！
但前面日志说 transition_splits 只在"从未真实激活"……我之前的推断："bd01 的四个分支各只指向单条转换（t07~t10 走 len(combos)==1 路径、从不真正拆分）"。这依据是 get_matched_dims 的 branches 过滤逻辑：for br in bd.get("branches", []): if br.get("target_transition") == t["id"]: vals.append(v)——t07 只被 branch "开题_合格及以上" 指向，所以 vals=["开题_合格及以上"]（单值）→ combos=1 → 单组合路径，不拆分。对！branches 过滤优先于 all values fallback。所以 t07 现状：matched_dims=[bd01]，但 vals 单值，combos=1，走单组合分支，保留原 id T-007，branch_path=[{项目归档分支: 开题_合格及以上}]。
好，那么加 rounds 虚拟维度后：t07 的 dim_values_lists = [(bd01, [开题_合格及以上]), (fake_rounds, [开题])] → combos = 1×1 = 1 → 仍走单组合路径，保留原 id。✓ 无害。
t02：现状 matched_dims=[]（t02 不在 bd01 coverage，note 无 branch_dimension）→ 无拆分，原 id T-002。加 rounds 后：matched_dims=[fake_rounds]，vals=["开题","验收"]（branches 过滤：fake_bd branches 的 target_transition 都是 t["id"]，两个值都命中）→ combos=2 → 多组合拆分路径 → 产出 T-002[a]（开题）、T-002[b]（验收），删除原 TO，to_index 留 synthetic main_to，transition_splits["T-002"]=["T-002[a]","T-002[b]"]。✓ 正确！
注入前置条件：既有代码在多组合路径里，对每个 combo 值 v：if v in state_lookup.get(t["entity"], {}).get(bd["dimension"], set()) ——fake_bd.dimension="项目阶段"，"开题"/"验收" ∈ E-XM.项目状态 states → 注入 state_ref precond {"text": "项目阶段=开题", "type": "state_ref", "ref": {E-XM, 项目阶段, 开题}} ✓ 完美复用。
变体 id：new_id = f"{t['id']}[{suffix}]" suffixes="abcdefghijklmnop" → T-002[a]/T-002[b]。字母后缀自动满足 F2！补丁不需要改这段——既有代码就是字母。F2 只需验证 patch 不引入中文后缀（交付报告里的 "T-002[开题]" 应该只是示意）。
t07~t10 与 bd01 的交互还有一个坑：t07 加 fake_rounds([开题]) 后 combos=1，走单组合分支——该分支只用 dim_values_lists[0]（bd01）的 branch_path，丢弃 fake_rounds 维度的 path，且不注入预条件（单组合分支不注入）。t07 原有"项目处于开题阶段"state_ref 兜底 → 语义无损。✓（这就是审查项 9，知情接受）
t05/t06 呢？t05 是 待评审→评审中（E-XM），within_rounds=双轮次。现状 matched_dims：t05 不在 bd01 coverage（bd01.branches target 是 t07-t10），note 无 branch_dimension → 无拆分。加 rounds → 拆成 T-005[a]/[b]。但 T-005 是 x01 镜像 XC 的 target_transition！x01 的 target_transition 经 _assign_ids 改写为 "T-005"（正式号）。P2 Step 3 的 process_constraint_lane 会 find_to_by_state(E-XM, 项目状态, 评审中)——遍历 to_index：T-005[a] 和 T-005[b] 的 to 都是"评审中"！find_to_by_state 返回第一个匹配——遍历 dict 顺序，可能返回 T-005[a]。或者 synthetic main_to（T-005 本身）也在 to_index（多组合路径里 to_index[t["id"]] = main_to 保留）。find_to_by_state 遍历 to_index.items()，T-005 键先插入（在变体之前），会命中 synthetic main_to。main_to 是完整字段的对象 → CO 构造正常。下游 S0/S1 消费 CO.dependent_transition_id="T-005" 时用 transition_splits 解析到 [a]/[b] 两个变体。这正是 Option C 的设计。✓
好，管线推演通畅。补丁还有一个关键点：fake_bd 不能污染 p1_bd（全局列表）——只 append 到 matched_dims 局部。既有代码 dim_values_lists 从 matched_dims 构建，branches 过滤用 br.get("target_transition") == t["id"]——fake_bd 的 branches target_transition 设为 t["id"] 即可。combo_conflicts_with_preconds 用 combo_map[dim_name]=val 检查 precond——t02 的 precond 无项目阶段相关文本，无冲突。t03（已选入→待选入）precond"评审管理员只能对已选入状态的项目进行选出"——无项目阶段词。t04 precond"评审管理员从处于已选入状态的项目中选取1-5个项目纳入评审计划"——无。t05 precond"评审计划状态由待启动变为待评审"——无。t06 precond"评审组全部专家提交了项目打分"+"自动进行项目评价"——无。t11 precond"评审计划状态变为取消结束"——无。t11b precond"评审计划状态由已完成变为结束或超时结束"+"项目处于评审中状态"——无项目阶段词。✓ 全部无冲突，R5 不误滤。
还有 within_rounds 从哪读？P2 读的是 p1["state_and_flow"]["transitions"] 的 t dict——前提是 P1 落盘了 within_rounds（F1）。review_structured.json 的 transitions 由 model.py add_trans 构造 → t dict 必须含 "within_rounds" 键。
还有 t01？不加 within_rounds（创建转换豁免）。校验规则也要豁免 from=None。
再看 state_info kind 透传（F3）：state_info 构造在 P2 Step 5（截断区，约 2562 行前）。补丁脚本无法看到确切代码……这是难点。补丁脚本采用"锚点插入"方式：找到 Step 5 state_info 构造段。但我没有 Step 5 的源码（截断了）。怎么办？
替代方案：s4 补丁不依赖 state_info.kind，而是从 coverage model 的 transition_obligations + 实体维度重新推导 rounds 维度：S4 补丁可以读 cm["_context"]["state_info"]——如果 kind 没透传，S4 拿不到。另一个途径：S4 补丁自己从 TO 的 preconditions 里检测"项目阶段=开题"注入的 state_ref？太 hack。
更稳妥：patch_a2_rounds.py 增加一个通用后处理函数——在 P2 输出 JSON 落盘前，对 _context.state_info 的每个维度注入 kind。怎么注入？state_info[entity].dimensions[] 与 p1 实体的 state_dimensions 对齐（dimension_name 匹配）→ 从 p1 实体维度拷贝 kind。但补丁脚本插入位置在 Step 2 循环内……后处理需要在输出组装处（约 3000 行 _context 写出附近）。
简化方案：patch_a2_rounds.py 的 apply_patch() 做两处插入：

Step 2 循环内 matched_dims 之后（rounds 虚拟维度注入）
输出落盘前（_context 组装后）：给 state_info 补 kind —— 用正则找 "state_info" 构造点不可靠。

另一个更简单的方案：S4 补丁不读 state_info.kind，而是读 p1 的……不，S4 只有 coverage model。
再想：state_info 构造代码我看不到，但它的数据源必然是 p1 domain_model.entities 的 state_dimensions（P2 从 P1 派生）。如果它是 for e in p1[...]["entities"]: for dim in e.get("state_dimensions", []): ... dimensions.append({...选择性字段...}) 的形态，那么补丁可以在 state_info 构造后统一后处理。最稳的锚点：输出 JSON 的顶层组装处（"transition_splits": transition_splits, 所在的 dict——我看到了，在 3004 行附近）。在该 dict 组装前插入一段后处理代码：遍历输出 dict 的 _context.state_info，对每个 entity 的每个 dimension，从 p1 实体同维度拷贝 kind/progression/scoped_to。
但 _context 在哪组装？3004 行的代码片段是 "pipeline_trace": ... }, "branch_dimensions": bd_enriched, "transition_splits": transition_splits, ...——这像是 _context 或顶层的一部分。锚点可用 transition_splits = transition_splits（3004 行 "transition_splits": transition_splits,）。在它前面插入后处理循环。
实际上还有个更干净的做法：补丁在 Step 0 之后（state_info 索引构建处附近，约 200 行）就构造一个 rounds_dims 全局映射 {entity_id: rounds_dim_name}，然后在输出组装锚点处注入。或者干脆在 matched_dims 注入点同时把 fake_bd 的 kind 信息写入一个全局 dict rounds_split_mark = {}，供 S4 用……不行，S4 读的是输出 JSON。
最简可行：两处插入。
插入点 A（Step 2 循环，matched_dims = get_matched_dims(t) 之后）：rounds 虚拟维度注入 + rounds_split judgment 标记（F4）。
插入点 B（输出组装，"transition_splits": transition_splits, 之前）：state_info kind 后处理——遍历 p1 entities 的 state_dimensions，构建 {entity: {dim_name: kind}}，然后注入到输出的 state_info。但插入点 B 处的变量名（state_info 所在结构）未知……
风险太高。换思路：S4 补丁改为从 transition_obligations 推断 rounds 维度——任何 TO 的 preconditions 里有 {"ref": {"dimension": D, ...}, "pattern": "branch_injected"} 或 branch_path 含维度 D 的注入，且该维度在多个 TO 上以"轮次成对"出现……不可靠。
再换：其实 S4 只需要知道"哪些维度是 rounds 从而排除"。等价信息：_context.state_info 的维度 kind。如果 P2 透传不了，可以让 P1 的 review_structured.json 直接带 kind（F1 保证 add_entity dims 透传 kind——model.py 的 dims 构造加了 kind），而 state_info 若是从 p1 维度 dict 复制构造……若构造代码是 dim 整体拷贝或字段白名单，白名单会丢 kind。
务实决策：patch_a2_rounds.py 的 verify_insertion() 加一项检查：运行后 grep 输出 JSON 的 state_info 是否含 "kind"。若不含，fallback：S4 补丁用硬编码？不行——领域硬编码被我明确禁止（"S4 排除 rounds 维度必须按 kind 字段排除，按维度名排除就是领域硬编码"）。
最终方案：补丁 B 点。让我赌一个合理锚点：P2 的 state_info 构造。从 Step 5 注释"Step 5: _context (incl. phase_mapping per (entity, dim))"和 Step 6 "The phase_mapping is written to _context.state_info[entity].dimensions[i].phase_mapping"——state_info[entity] 有 dimensions[i] 列表。Step 6 代码在 2562 行后。构造很可能在 Step 5。补丁可以：在 Step 6 phase_mapping derivation 之前/之后插入后处理：遍历 state_info（全局变量），对每个 entity/dimension，从 p1_entities_by_id 查同维度，拷贝 kind。
锚点文本：# ============ Step 6: phase_mapping derivation ============（2562 行，我确认过存在）。在它之前插入后处理代码块。后处理代码引用全局 state_info 变量——变量名不确定！Step 5 构造的变量可能叫 state_info 或别的。从 Step 6 docstring："is written to _context.state_info[entity].dimensions[i].phase_mapping"——说明输出路径是 _context["state_info"]。全局变量名很可能是 state_info。
补丁的 verify 步骤（apply 后 py_compile + 试运行）能兜底：如果变量名不对，运行报 NameError，补丁脚本报告失败。这是可接受的工程折衷——补丁脚本本身就是"运行-验证-报告"模式。
好。S4 补丁（patch_a2_s4_multi_instance.py）：

插入点：entity_dim_count 构建处（entity_dim_count[e] = len(entity_dim_count[e]) 后或 dim 收集循环内）
逻辑：构建 rounds_dim_names = set()：遍历 cm["_context"]["state_info"]，收集 d.get("kind")=="rounds" 的 dimension_name（按实体）。然后在 entity_dim_count 收集循环里：if d in rounds_dims_for_entity: continue。具体：S4 的循环是 for to in tos: e = to.get("entity"); d = to.get("dimension"); if d: entity_dim_count.setdefault(e, set()).add(d)。要按实体排除：先从 state_info 建 {entity_id: set(rounds_dim_names)}，循环里 if d in rounds_dims.get(e, empty): continue。
VE dim_count 同理跳过。



DECISIONS.md（F8）​：追加条目，格式对齐（我没看过实际格式，给出通用格式：日期+编号+决策+理由+影响）。写成片段文件 DECISIONS_entry_a2.md 让用户手动追加，或直接给出追加内容。
prompt.md：已交付过一版。这次要输出完整版吗？prompt.md 369 行（7104 tokens），输出完整版可行。需要包含：Step 1 状态维度小节后加"维度类型"判定（轮次型三问）、Step 4 within_rounds 语法、API 签名 add_trans 加 within_rounds、add_entity dims 注释加 kind/progression/scoped_to。
现在考虑输出方式：这是大文件生成任务，应该走 agent_v2_subagent。任务描述要把上述全部规格写清楚。用户说"输出完整代码"——工程代理会产出文件并报告。
文件清单：

srs_pipeline/prompt.md（完整，~400行）
srs_pipeline/model.py（完整，~550行）
srs_pipeline/schema.py（完整，~300行）
srs_data/struct_srs.py（完整，~2700行，30852 tokens——大文件，代理需基于我提供的规格重写；代理环境里可能没有原文件……之前代理成功写过 struct_srs.py（Goals 1-4），说明代理有办法。这次任务描述里要说明：struct_srs.py 需在既有交付基础上补 F5/F7 修正（4.8.1(7) BR、4.9.3 op/属性、t07 er 清理、t41 precondition 修正、t01 不加 within_rounds、头部注释更新）
patch_a2_rounds.py（完整补丁脚本，含 F2/F3/F4 修正）
patch_a2_s4_multi_instance.py（完整，含 kind 排除）
DECISIONS.md 追加条目（F8）

由于 agent 环境之前已产出 /mnt/work 下的文件，这次是"重新编辑修改"，代理可以基于自己上一轮的产出迭代。任务描述要包含全部 F1-F8 规格和验收断言。
任务描述要非常详细。让我写。
关键规格汇总（给代理）：
F1 — model.py add_trans 落盘：
pythondef add_trans(self, tid, entity, dimension, frm, to, action, role,
              preconditions, expected_results, traits, direction,
              priority, source_ref, note=None, within_rounds=None):
    validate_llm("trans", {..., "within_rounds": within_rounds})
    t = {..., "within_rounds": list(within_rounds or [])}


空值规范：字段必须存在，无内容传 [] → "within_rounds": list(within_rounds or [])。
F6 — model.py assemble 完备性校验（放 assemble() 里，_assign_ids 后、validator 前；或作为独立方法 _validate_rounds）：

收集 rounds 维度：{entity_id: dim_dict}（kind=="rounds"）
校验项：
a. kind=="rounds" → progression 必填、非空；set(progression)==set(states)（集合一致，顺序表达推进序）
b. scoped_to 必填且为同实体的另一维度名
c. 该实体 scoped_to 维度上的转换：from!=None 的必须带非空 within_rounds → 缺失即 error（fail-fast，进 report.error 或 raise CriticalAmbiguity——用 report.error 走既有校验通道更合规）
d. rounds 维度自身（项目阶段）的转换不得带 within_rounds
e. within_rounds 值 ⊆ rounds 维度 states
注意既有校验体系：Validator(self, output).run_all() + report.error("CXX", msg)。轮次校验可作为 model 内置检查在 assemble 中向 report 加 error（用新检查号如 C17 或直接 error("rounds", ...)）。看 model.py：report.error(e.check, e.message)——check 是字符串。我们用 "C17-rounds" 之类。简单起见：在 assemble 里 self._check_hooks 之外直接判断并 raise CriticalAmbiguity？但 CriticalAmbiguity 是"校验修复失败→中断"。更贴现状：把校验做成 on_before_assemble 之后、_build_output 之前的手动检查，失败 raise ValueError（fail-fast，与 validate_llm 同级）。工程上 ValueError 更直接。

F7 — struct_srs.py：

t07: within_rounds=["开题"]；expected_results=["若项目归档分支=开题_合格及以上，则项目状态由待归档变为已选入"]（删除"，阶段由开题变为验收"）
t41: preconditions 重写：
python生成中preconditions=[
    precond(text="评审计划状态由已完成变为结束或超时结束", ptype="state_ref",
            ref=state_ref(entity="E-PSJH", dimension="评审计划状态", state="结束"),
            note={"comment": "跨实体precondition：评审计划归档完成；另可匹配超时结束"}),
    precond(text="项目处于开题阶段", ptype="state_ref",
            ref=state_ref(entity="E-XM", dimension="项目阶段", state="开题")),
    precond(text="开题评级在合格及以上", ptype="constraint",
            note={"comment": "评级为派生属性非状态维度，降级constraint"}),
],




F5 — struct_srs.py：

E-PSJH attributes 加：attr(name="打分进度", desc="打分已提交专家人数占评审专家总人数的比例")
E-PSJH operations 加：op(name="查看打分进度", category="query", expected_results=["打分已提交专家人数占评审专家总人数的比例"], source_ref="4.9.3（1）", note=N(role=["评审管理员","评审助理"], comment="通用操作"))
"查看评审计划执行信息" op 的 source_ref 改 "4.8-1 #14；4.9.3（2）"，expected_results 改 ["提供该计划中项目的状态和打分情况", "展示评审组各专家对具体项目各打分项的打分记录及该项目的得分统计及评价结果"]？——小心：改 expected_results 会影响 2.5b covered_by 等。最小改动：source_ref 追加 "；4.9.3（2）"，expected_results 保持。嗯，还是保守：只加 source_ref 追加。
BR b69（4.8.1(7)）：
python生成中m.add_br(
    bid="b69",
    category="timing",
    desc="只要计划结束，评审专家与计划相应的评审时段为可用",
    entities_involved=["E-PSJH", "E-ZJ"],
    constrained_entity="E-ZJ",
    source_ref="4.8.1（7）",
    signal_type="restrictive",
)


位置：b68 之后。头注释 BR：b01-b69。
t01 不加 within_rounds（创建转换豁免）。11 条转换声明（t02-t06 双轮次、t07/t08 开题、t09/t10 验收、t11/t11b 双轮次）。

等等——t03（项目选出，已选入→待选入）双轮次？验收轮的已选入项目可以选出吗？语义上应该可以（验收轮已选入→待选入，阶段不变）。t11/t11b 双轮次合理。t03 双轮次合理。OK 保持冻结方案的 11 条。
F2/F4 — patch_a2_rounds.py：


变体后缀沿用既有字母约定（不改动该段，验证即可）


rounds 展开独立 judgment：注入 fake_bd 时 add_judgment("rounds_split", ...)——但 add_judgment 在补丁插入点可用（全局函数）。fake_bd 注入处打标：add_judgment("rounds_split", f"{t['id']}: within_rounds={...} 注入轮次虚拟维度 {dim_name}，按既有分支拆分管线展开")


fake_bd 不进 p1_bd，只进 matched_dims


单值 within_rounds 也注入（t07-t10）：matched_dims 变化后走既有路径；t07 因 bd01 branches 过滤单值 × rounds 单值 = 1 组合 → 单组合分支保留原 id ✓


F3：state_info kind 后处理，锚点 # ============ Step 6: phase_mapping derivation ============ 之前插入：
python生成中# A2-F3: state_info kind 透传 —— 从 P1 实体维度拷贝 kind/progression/scoped_to
# （state_info 构造若为字段白名单会丢 kind；此处统一后处理，S4 据此排除 rounds 维度）
for _ent, _info in (state_info or {}).items():
    _p1e = p1_entities_by_id.get(_ent) or p1_entities_by_id.get(_info.get("entity_id", _ent))
    ...维度名对齐拷贝 kind...


变量名 state_info 不确定——补丁 verify 阶段试运行兜底。另外还要处理：state_info 键可能是 entity_id，info 里可能有 entity_name/dimensions。后处理按 dimensions[].dimension_name 对齐 p1 实体 state_dimensions。
还有一个更稳的锚点：在 transition_splits[t["id"]] = split_ids 之后全局处不好。就用 Step 6 锚点。备选锚点："transition_splits": transition_splits,（3004行，输出组装）——在它前面插入后处理，作用于将要输出的 _context。但 _context 变量名也不确定……Step 6 锚点 + state_info 变量名猜测 + verify 兜底。或者双锚点尝试。任务描述里写清楚：优先 Step 6 注释锚点，若 state_info 变量名不符则报错并提示人工确认。
其实还有个思路：完全绕开 state_info——S4 补丁从 cm["_context"] 拿不到 kind 就从 TO preconditions 拿：注入的轮次 precond 有 pattern="branch_injected"？看代码：branch_injected pattern 只在 extra_refs（precondition_state_refs）里，precond 本身无 pattern 键。注入的 precond text 是 "项目阶段=开题"。S4 可以从 TO 的 preconditions 找 ref.dimension 相同且 text 形如 "{dim}={val}" 的注入痕迹……复杂且脆。
坚持方案：patch_a2_rounds.py 负责保证输出 JSON 的 state_info 带 kind（插入点 B），patch_a2_s4 读 kind。verify 链兜底。


S4 补丁 — patch_a2_s4_multi_instance.py：

插入点1：entity_dim_count 构建循环后（for e in entity_dim_count: entity_dim_count[e] = len(entity_dim_count[e]) 之前）：
python生成中# A2: rounds 维度不计入 dim_count（轮次语义由 TO 变体承载，匿名实例展开与轮次无关）
_rounds_dims = {}  # entity_id -> set(dim names)
_si = (cm.get("_context") or {}).get("state_info", {}) or {}
for _ent, _info in _si.items():
    for _d in (_info.get("dimensions") or []):
        if _d.get("kind") == "rounds":
            _rounds_dims.setdefault(_ent, set()).add(_d.get("dimension_name", ""))
for to in tos:
    e = to.get("entity", "")
    d = to.get("dimension", "")
    if d and d in _rounds_dims.get(e, set()) and e in entity_dim_count:
        entity_dim_count[e].discard(d)


放在 len() 计算前。注意原循环 for to in tos: ... entity_dim_count.setdefault(e, set()).add(d) 然后紧接 for e in entity_dim_count: entity_dim_count[e] = len(...)。插入在两者之间做 discard。
VE dim_count：ve_dim_count 循环同样排除 rounds（VE dims 来自 VE transitions 的 dimension——rounds 维度的转换（t40/t41）若属于 VE……本例无 VE，通用起见同样过滤）。
entity_instances 不变（公式自动用新 dim_count）。

DECISIONS.md 条目（F8）​：
code## D-2026-08-19-A2 轮次型状态维度（kind="rounds"）
背景：项目评审系统 E-XM 的"项目阶段"（开题/验收）被建模为平级正交维度，
流转转换（选入/纳入/进入评审/完成打分）为单条 stage-agnostic 边，
导致验收轮次无独立 TO、用例静默复用开题轮（PROC-108/110/164）。
决策：新增轮次型维度语义——state_dimensions 支持 kind="rounds"（progression 声明推进序、scoped_to 声明作用主维度）；scoped 维度转换声明 within_rounds，P2 Step 2 复用 Option C 变体展开管线按轮次拆分 TO（字母后缀 [a]/[b]），注入 state_ref 前置条件，transition_splits 登记抽象→变体映射；P1 assemble fail-fast 强制 rounds 实体的 scoped 维度非创建转换必须声明 within_rounds（堵静默复用复发）；S4 dim_count 排除 rounds 维度。
取舍：t41 保留为轮次推进权威边，t07 expected_results 去除阶段变更语句（消除双重记账）；计划类型（E-PSJH is_config）与项目阶段的相关性未结构化（遗留）；本轮顺带回归修复 4.8.1(7)（b69）与 4.9.3（打分进度 op/属性）。
影响：TO 42→~46；E-XM S4 实例 5→4；验收轮获得专属用例。
