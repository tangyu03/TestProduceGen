# PT017 语义问题根因分析

对 temp.md 提出的 6 类问题逐项追溯（数据取自本次 PT017_output，代码取自 S1/S0/P2/replay）。

> 前置说明：temp.md 的用例编号对应**更早一版**输出（如它列 PROC-049=密码锁定、PROC-062=部门管理等），本次输出编号已漂移。但问题**类别**全部仍在，本文用本次实际编号对照（temp.md 编号 → 本次编号）给出根因。

---

## 根因 1（最底层）：C 级二级审批从未建模 —— 结构层缺口

**现象**：C 级任务只走"待审批 → 审批通过（一级审批员）"，无二级审批；二级审批员永不出现。

**证据链**：
- SRS builder（`srs_data/CASC-STEC-PT017.py`）为 9 个任务实体各定义了 1 个审批转换（t02/t08/t14/t20/t26/t32/t38/t44/t50），全部是 `frm="待审批", to="审批通过", action="审批通过", role="一级审批员"`。
- t02 的 note 写 `"C级需二级审批员（t02b）"`——**t02b 从未创建**。"待二级审批"状态、二级审批员转换在结构上不存在。
- 第 1965 行注释把 `_meta.branch_dimensions` 里的 `"二级审批通过转换"` 明确回填为 t02：
  > `"二级审批通过转换" → t02（C 级二级审批在同 frm/to 上，作为分支差异由 BR 兑现）`

  即：C 级二级审批被**降格为"同状态上的分支差异"**，靠 BR-011 提示文字表达，不进状态机。
- P2 将 t02 分裂为 T-002[a]（B级）/T-002[b]（C级），但 [b] 继承的仍是**一级审批员** role——源转换只有一个 role。

**为什么 V01-V10 全 PASS**：BR-011 的"B级需一级审批；C级需二级审批"只是约束文本，不在结构层；V01-V10 是结构校验，抓不到文本/结构分裂。

---

## 根因 2：标题生成对分支不感知 → 18 条标题与分支条件倒置

**现象**：如 PROC-156（T-044[b]，givens 分支=任务级别=C级）标题却是"任务处于待审批状态且级别为**B级**时…"；PROC-158（T-002[b]，C级）标题="B级导入任务…"；PROC-159（T-008[a]，B级）标题="C级登记任务…"。

**证据链**：
- 标题由 LLM（glm-4-flash，`main.py:_generate_titles`）**按每个基础转换生成一次**，prompt（`prompts/title_prompt.py`）是通用"条件+动作+结果"模板，**不强制注入 branch_path 值**。
- LLM 标题锚定在实体的"典型级别"：导入/导出/扫描/归档/外送/回收/留存/移交→"B级"，登记→"C级"，与真实分支无关。
- `scripts/s1_fix_replay.py` 的 `_content_key`（line 127）把 source_ids 去掉 `[a]/[b]` 后缀归一化，且**不含 givens** → [a]/[b] 两个分支变体命中**同一条归档标题**，回放时旧标题（B级锚定）被直接覆盖到 C 级分支上。
- 实测对比 `_title_compare.md`：ARCH title == REPL title（[b] 分支的 ARCH givens 已含 `任务级别=C级`），证明标题来自回放覆盖而非按分支重生成。

---

## 根因 3：锁定转换被误判为"负向用例" → 自相矛盾的拒绝断言（temp.md PROC-049 → 本次 PROC-057）

**现象**：PROC-057（T-061，连续输错密码锁定）同时断言
- THEN1 `操作被拒绝，状态保持为正常并给出禁止提示`（br_refs=[BR-002]，post_state=正常）
- THEN2 `用户连续输错3次密码，系统自动将此用户锁定`

两条互斥。而同一转换的 PROC-058 是正确版本（正常→锁定）。

**机制链（全部确认）**：
1. P2 `prohibition_config.prohibit_keywords` 配了 **`连续输错3次密码`**——这是 t63 的**触发条件**，不是禁止词。
2. `nodes/s1_generation.py` guard-polarity 检测（line 1619-1636）：
   - BR-002（signal_type=restrictive）描述含该短语 → 过禁止词闸；
   - 实体 E-USER ∈ entities_involved → 命中；
   - 8 字符滑动分块命中 givens；
   - **action 兜底 `action_core_clean[:2] in _br_desc`（line 1632-1633）**：action"连续输错密码锁定"前 2 字"连续"出现在 BR-002 描述里 → `is_negative_branch=True`。
3. 负向分支输出 `操作被拒绝，状态保持为正常…`（line 1646），`post_state=from_state=正常`。
4. **side_effects 嵌入循环（line 1720-1744）位于 if/else 之外、无条件执行**——t63 的 side_effect `系统自动将此用户锁定` 被追加为 behavior Then。
5. 结果：同一条用例断言"状态保持正常"又断言"自动锁定"。

**修复点**：① 禁止词配置移除触发条件词（"连续输错3次密码"）；② 负向分支下跳过/改写 side_effect 嵌入；③ action 匹配去掉 2 字前缀兜底，改用完整 action 或 `_br_ops`。

---

## 根因 4：BR 过度嵌入 → 混合无关规则（temp.md PROC-062 → 本次 PROC-070）

**现象**：PROC-070（RO-IT-004，锁定→正常不可直接登录）内嵌了
- BR-019（用户账号格式：汉字/小写字母/数字/下划线，不可重复、不可修改）
- BR-032（内置 3 角色不可更改删除，绑定 sysadmin_a/secadmin_a/secauditor_a）

与"锁定用户不可直接登录"毫无关系。实测 `embedded_brs=["BR-019","BR-032"]`。

**机制链**：
- BR-019 描述含 `不可.*修改`、BR-032 含 `不可.*删除` → 都命中 `negative_test` 分类正则（line 2929）。
- negative_test 宿主选择（line 3654-3660）：`[p for p in procedures if p["obligation_type"] == 7 and p["entity"] in br_entities]`——**只按实体选宿主，不校验维度/操作语义**。IT-004 是 E-USER 上的 Type7 用例，于是把 E-USER 所有 negative_test BR 全收进来。

**修复点**：negative_test 宿主按 维度/状态/操作 对齐，不只按实体粗粒度匹配。

---

## 根因 5：CRUD / 规则用例无执行角色（364 条 actor 空）

**现象**：部分用例未指定执行角色。

**实测**（本次输出 727 条）：
| 义务类型 | 数量 | actor |
|---|---|---|
| Type1 转换 | 355 | 全有（一级审批员等） |
| Type3 属性配置 | 14 | 空 |
| Type6 CRUD | 269 | 空 |
| Type7 无效转换 | 14 | 空 |
| Type8 独立规则 | 67 | 空 |
| Type9 字段校验 | 8 | 全有（普通用户） |

空 actor 合计 **364**。

**证据链**：`PT_017_coverage_obligations.json` 的 entity_obligations 共 89 条，**带 role/actor/executor 字段的 = 0**。EO-CRU（CRUD 操作）义务数据模型里根本没有执行者字段；S1 对无角色字段的义务直接输出 `actor=""`。Type9 之所以有 actor，是因为字段校验义务单独带了角色。

**修复点**：SRS builder 为 EO-CRU/EO-ATC 义务补 role 字段（或 S1 对 CRUD 兜底为"普通用户/系统管理员"）。

---

## 根因 6：二级审批员永不出现

直接后果：9 个审批转换 role 全部 = `一级审批员`，数据中不存在"二级审批员"角色绑定，因此它永远不会作为执行者出现。与根因 1 同源。

---

## 修复优先级建议

1. **数据模型补建模（根因 1/5/6）**：为 C 级增加真实"待二级审批"状态 + 二级审批员转换（t02b）；为 EO-CRU/EO-ATC 补 role。两处都不动引擎。
2. **标题分支感知（根因 2）**：branch_path.value 注入标题 prompt；`_content_key` 纳入 givens/branch 键，使 [a]/[b] 不再共享归档标题。
3. **guard-polarity 收紧（根因 3）**：禁止词配置清理触发条件词；负向分支抑制 side_effect 嵌入；去掉 2 字前缀 action 匹配。
4. **BR 宿主语义化（根因 4）**：negative_test 宿主按 维度/操作 对齐，不只按实体。
